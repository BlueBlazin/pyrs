# Project Context: Python Interpreter in Rust

## Vision
Build a production‑grade Python interpreter in Rust with **full source and bytecode compatibility** with **CPython 3.14**, designed for long‑term evolution (JIT and extension support possible later), and minimal third‑party dependencies.

## Target Compatibility
- **Python version:** CPython **3.14**
- **Compatibility goals (now):**
  - Run Python source code.
  - Execute CPython 3.14 bytecode.
- **Non‑goals (now):**
  - JIT compilation.
  - Full CPython C‑API compatibility / C‑extensions.
- **Future‑friendly design:**
  - Keep architecture flexible so **JIT** and **extension support** can be added later.
  - Expect that popular packages (e.g., NumPy) typically need C‑extension support; plan for this later.

## Parsing
- Use CPython 3.14 grammar with a **packrat parser**.
- Grammar reference: https://docs.python.org/3/reference/grammar.html
- **Minimize dependencies**: prefer small, well‑scoped crates or bespoke implementations.

## Compiler & Execution Pipeline
- **AST → Bytecode IR** (CPython‑compatible bytecode).
- Bytecode IR should support **light optimizations** while preserving semantics.
- Design IR with a path to **JIT** in the future.

## Runtime Model
- Match CPython object model.
- **Reference‑counted GC** similar to CPython.
- Object identity uses stable `id()` values; `is`/`is not` are identity-based with a basic cycle GC for self-referential containers.
- **GIL** (single‑threaded execution model for now).
- GIL‑free execution is **not** a current goal.

## Standard Library
- Start with a **practical subset** of stdlib.
- Expand toward full coverage over time.

## Success Criteria
We measure success by:
- Running **real‑world apps**.
- Passing **CPython test suites** (as compatible as possible).
- Competitive **performance benchmarks**.

## Milestone Sketch (Iterative)
Roadmap milestones are now maintained in `docs/ROADMAP.md` and are structured to guarantee full CPython 3.14 parity with no "basic compat" completion criteria.

Current milestone state summary:
1. Milestones 0-3 complete (foundations: parser/AST, runtime identity+GC, bytecode intake, closures+frames).
2. Milestone 4 complete (P0): lazy generator suspension/resume + `yield from` send/throw delegation + `StopIteration` return-value propagation + close/`GeneratorExit` handling + reentrancy guard coverage.
3. Milestone 5 complete (P0): supported-path opcode hardening (explicit unsupported-opcode errors + decode/translation jump/stack validation) and supported-subset `.pyc` writer (header + marshal).
4. Milestone 6 complete (P0): import-system parity delivered for supported pure-Python scenarios (relative imports, namespace packages, `sys.path`/`sys.meta_path`/`sys.path_hooks`/`sys.path_importer_cache` contracts, module metadata/spec fields, `importlib` helper APIs).
5. Milestone 7 complete (P0): language-surface milestone delivered (decorators, assignment expressions, list/dict comprehensions + generator expressions with scope isolation, core `match`/`case` subset, async syntax lowering, `except*` parsing, type parameters on `def`/`class`, f-string lowering, and `__future__` placement/unknown-feature compile checks).
6. Milestone 8 complete (P0): runtime data-model semantics delivered for descriptor hooks, attribute-model hook paths, C3 MRO/`super(type, obj)`, and exception chaining metadata.
7. Milestone 9 complete (P0/P1): core runtime types (`set`/`frozenset`, `bytes`/`bytearray`/`memoryview`, `complex`), class metaclass keyword path + `__slots__` core behavior, and stdlib bootstrap foundations (including `codecs` and previously landed `random`) are implemented.
8. Milestone 10 complete (P1): core async/concurrency semantics are implemented for milestone scope (`async`/`await`, async generators, `aiter`/`anext`, foundational `asyncio`/`threading`/`signal` support).
9. Milestone 11 complete (P0/P1): parity/test gate foundations are active (CPython harness suites + differential CPython checks + parser/VM fuzzing + curated real-world smoke suite + parity profile script).
10. Milestone 12 complete (P0): current curated CPython harness language/import suites pass with an empty allowlist.
11. Remaining milestones 13-16 cover: long-tail language/runtime parity + stdlib/packaging usability closure, performance/observability/runtime hooks, native-extension ecosystem compatibility, and release hardening/certification.
12. Strict Milestone 13 P0 blockers now explicitly include full `json`, `_csv`/`csv`, and `pickle`/`pickletools`/`copyreg` semantic closure with robustness/performance proof; these are tracked in `docs/STUB_ACCOUNTING.md`, `docs/ROADMAP.md`, and `docs/PRODUCTION_READINESS.md`.
13. Curated CPython language and import harness suites currently pass with empty allowlist (including `test_enum.py`, `test_pkgutil.py`, and `test_importlib/resources/test_resource.py` import paths); strict stdlib unittest lane is active in harness (`tests/cpython_suite_strict_stdlib.txt`) with owned entries in `tests/cpython_allowlist_strict.txt` (currently `stdlib-strict-pickle` and `stdlib-strict-pickletools`). Strict lane execution is opt-in for frequent local loops (`PYRS_RUN_STRICT_STDLIB=1` or `PYRS_PARITY_STRICT=1`) and still exercised by parity-gate runs. For frequent strict reruns, allowlisted strict entries are skipped by default; set `PYRS_RUN_ALLOWLISTED_STRICT=1` to execute them. Allowlisted strict entries now use a separate timeout budget (`PYRS_STRICT_ALLOWLIST_TIMEOUT_SECS`, default 30s) so allowlisted-enabled profiling loops stay bounded (~65s total instead of ~240s) while remaining blockers stay visible.
14. Engineering quality-gate policy is now explicit (`docs/ENGINEERING_GATES.md`) with a tracked algorithmic/semantic backlog (`docs/ALGO_AUDIT_BACKLOG.md`); Milestone 13 completion requires all P0 audit items closed.

## Engineering Principles
- Maintain correctness first, then performance.
- Keep dependencies minimal and well‑justified.
- Prefer clear architecture boundaries: parser, AST, compiler/IR, VM, runtime, stdlib.
- Commit frequently in small, focused checkpoints to minimize loss on interruption/crash.
- Do not leave long-lived dirty worktrees: commit after each completed logical step and before any full-suite run.
- At the end of every assistant turn, always include the immediate next `3-6` concrete steps.

## Project Artifacts
- Roadmap: `docs/ROADMAP.md`
- Milestone 12 completion report: `docs/MILESTONE_12_BACKLOG.md`
- Compatibility tracker: `docs/COMPATIBILITY.md`
- Production readiness accounting: `docs/PRODUCTION_READINESS.md`
- Stub and partial implementation ledger: `docs/STUB_ACCOUNTING.md`
- Pure-Python stdlib migration map: `docs/STDLIB_MIGRATION_PLAN.md`
- CPython dict mapping note: `docs/DICT_BACKEND_CPYTHON_MAPPING.md`
- Dict backend benchmark note: `docs/DICT_BACKEND_BENCHMARK.md`
- Engineering quality gates: `docs/ENGINEERING_GATES.md`
- Algorithmic and semantic audit backlog: `docs/ALGO_AUDIT_BACKLOG.md`
- Clone audit baseline/report: `docs/CLONE_BASELINE.txt`, `docs/CLONE_AUDIT.md` (refresh via `scripts/clone_audit.sh`)
- Coverage gate script: `scripts/run_coverage_gate.sh` (`cargo llvm-cov` summary; CI enforces soft floors: regions 70%, functions 65%, lines 70%; script defaults now encode 70/65/70 when enforcement is enabled)
- Dict backend benchmark script: `scripts/bench_dict_backend.sh`
- Native stdlib VM implementations: `src/vm/stdlib/`
- P0 critical-path note: `json`, `_csv`/`csv`, and `pickle`/`pickletools`/`copyreg` are explicit release blockers until full parity + robustness/perf gates are closed.
- Generated `NoOp` inventory: `docs/NOOP_BUILTIN_INVENTORY.txt` (refresh with `cargo run --quiet --bin print_noop_inventory > docs/NOOP_BUILTIN_INVENTORY.txt`)
- `NoOp` inventory traversal is recursive across module/class/instance/container graphs.
- Current `NoOp` inventory is reduced to test-only `_testcapi`/`_testinternalcapi` stubs plus `sys.monitoring` and `sys._jit` scaffolding.
- CPython vendor sync script: `scripts/sync_cpython.py`
- Vendor snapshot: CPython 3.14.3 grammar + opcode sources synced into `vendor/cpython-3.14/` (opcode table CSV generated).

## Current Scaffolding (Early Stage)
- Parser: packrat-style memoization with indentation tokens, explicit backslash line-join handling, Unicode identifiers, and soft-keyword disambiguation (`match`/`case`/`type`); statements include `if`/`elif`/`else`/`while`/`for`, `break`/`continue`, `with` (including multi-item forms), `match`/`case` (literal/capture/guard plus sequence/mapping/class/or/as/star families, class-pattern positional-after-keyword rejection, correct `or`/`as` precedence (`A | B as n` -> `(A | B) as n`), and statement-grade error surfacing for invalid `match ...:` forms), `try` with `except*` parsing plus comma-separated exception types in `except` handlers (`except A, B:`), decorators, `def`/`class` (including type parameters on headers and class-header `metaclass=` keyword args), async statement forms (`async def`/`async for`/`async with`), imports, annotation syntax, chained assignment, and `del`. Expressions include assignment expressions (`:=`), list/dict comprehensions + generator expressions, await syntax, `yield`/`yield from`, starred tuple/list displays, implicit adjacent string literal concatenation, and f-string lowering.
- Bytecode: minimal opcodes for source compiler plus CPython 3.14 decoder/translator (`opcode_table.csv` from CPython 3.14).
- Compiler/VM: emits and executes bytecode for prior core feature set plus Milestone 7/10 additions: decorator application lowering, assignment expressions (`:=`), list/dict comprehensions and generator expressions via isolated synthetic function scopes, core `match`/`case` lowering (literal/capture/guard plus compile-time validation for duplicate capture names, OR-alternative binding-set parity, and irrefutable-pattern reachability rules), async statement/expression runtime semantics (`async def`/`await`/`async for`/`async with`, `GET_AWAITABLE`, coroutine/async-generator protocol hooks, `aiter`/`anext`), and async-for exhaustion handling that clears `StopAsyncIteration` before loop exit (preventing stale active-exception leakage into following calls). F-string lowering to concatenation + `str(...)` conversion, chained assignment lowering (single RHS evaluation + multi-target stores), generalized subscript assignment targets (non-name container expressions), and delete opcodes (`DeleteName`/`DeleteAttr`/`DeleteSubscript`) are implemented. Arithmetic now includes true division (`/`) and float-aware semantics for `/`, `//`, `%`, `**`, and unary `+`/`-`. Builtin `exec` now executes source strings and code objects with explicit globals/locals namespace handling (including closure arity/type validation for code-object exec). Class invocation now routes through metaclass `__call__` when present before default instance-construction fallback; type-derived metaclass invocation now constructs class objects through class-builder paths (instead of instance fallback), and `type(ClassObj)` reports the class object's resolved metaclass for non-builtin metaclasses. Runtime `==`/`!=` dispatch now resolves dunder compare methods through bound-special lookup (avoids unbound class compare call-shape mismatches in stdlib paths). Compiler lowering now defers `return` inside `try` blocks through `finally` epilogues so cleanup paths always run (including nested `try/finally` and `try/except/finally` flows). `__future__` compile checks enforce top-of-file placement and unknown-feature rejection, future-import statements are compile-time-only (no runtime import side effects), and baseline `from __future__ import annotations` defers annotation evaluation to strings.
- Compiler/VM: emits and executes bytecode for prior core feature set plus Milestone 7/10 additions: decorator application lowering, assignment expressions (`:=`), list/dict comprehensions and generator expressions via isolated synthetic function scopes, core `match`/`case` lowering (literal/capture/guard plus compile-time validation for duplicate capture names, OR-alternative binding-set parity, and irrefutable-pattern reachability rules), async statement/expression runtime semantics (`async def`/`await`/`async for`/`async with`, `GET_AWAITABLE`, coroutine/async-generator protocol hooks, `aiter`/`anext`), and async-for exhaustion handling that clears `StopAsyncIteration` before loop exit (preventing stale active-exception leakage into following calls). F-string lowering to concatenation + `str(...)` conversion, chained assignment lowering (single RHS evaluation + multi-target stores), generalized subscript assignment targets (non-name container expressions), and delete opcodes (`DeleteName`/`DeleteAttr`/`DeleteSubscript`) are implemented. Arithmetic now includes true division (`/`) and float-aware semantics for `/`, `//`, `%`, `**`, and unary `+`/`-`. Builtin `exec` now executes source strings and code objects with explicit globals/locals namespace handling (including closure arity/type validation for code-object exec). Class invocation now routes through metaclass `__call__` when present before default instance-construction fallback; type-derived metaclass invocation now constructs class objects through class-builder paths (instead of instance fallback), and `type(ClassObj)` reports the class object's resolved metaclass for non-builtin metaclasses. Runtime `==`/`!=` dispatch now resolves dunder compare methods through bound-special lookup (avoids unbound class compare call-shape mismatches in stdlib paths). Compiler lowering now defers `return` inside `try` blocks through `finally` epilogues so cleanup paths always run (including nested `try/finally` and `try/except/finally` flows). Internal attribute-call paths now preserve caller frame state during descriptor/`__getattribute__`/`__getattr__` dispatch to avoid incorrectly swallowing exceptions into surrounding `try` handlers, and `object.__getattribute__`/`object.__setattr__`/`object.__delattr__` now propagate descriptor/handler exceptions instead of returning `None`. `__future__` compile checks enforce top-of-file placement and unknown-feature rejection, future-import statements are compile-time-only (no runtime import side effects), and baseline `from __future__ import annotations` defers annotation evaluation to strings.
- VM decomposition progress: arithmetic/comparison/type-union operator kernels are extracted into `src/vm/ops.rs`; native stdlib VM handlers for `json`, `re`, and `_csv`/`csv` are extracted into `src/vm/stdlib/{json,re,csv}.rs`; pickle object-protocol helpers (`object.__getstate__`, `object.__reduce_ex__`) are extracted into `src/vm/stdlib/pickle.rs`; and CSV helper kernels (`validate_csv_parameter_consistency`, row parser/quote logic, char/value coercion helpers) are now moved out of `src/vm/mod.rs` into `src/vm/stdlib/csv.rs` with direct module-local unit coverage.
- Runtime data model (Milestones 8-9 + 13 progress): descriptor-aware attribute load/store paths (`__get__`, `__set__`, `__delete__`), instance hooks (`__getattribute__` override baseline plus `__getattr__`, `__setattr__`, `__delattr__`), C3 MRO metadata (`__mro__`, `__bases__`), `super(type, obj)` lookup object plus baseline zero-argument `super()` runtime path, class-header `metaclass=` path, `__build_class__` metaclass kwarg handling, metaclass conflict detection/selection across bases, class-object metaclass fallback attribute lookup, `__slots__` restrictions (including empty slots and `__dict__` slot behavior), and builtin attr helpers (`getattr`, `setattr`, `delattr`, `hasattr`, `object.__getattribute__`).
- Container-internals caveat (P0/P2 carry-forward): runtime `dict` now uses a dedicated open-addressing probing backend (CPython-style empty/dummy/occupied slots with perturb probing) with insertion-order entry storage; `set`/`frozenset` remain hash-indexed runtime container objects with insertion-order backing vectors and compact hash buckets. Core unhashable key/item rejection is enforced on constructor/update/assignment/membership flows, literal dict construction (`{...}`), `dict.fromkeys(...)`, and `collections.Counter(...)`; dict equality is insertion-order independent and set/frozenset equality is value-based (including cross-type equality). Remaining CPython hash/equality edge semantics and hot-path performance closure are tracked in `docs/STUB_ACCOUNTING.md`, `docs/PRODUCTION_READINESS.md`, and `docs/ROADMAP.md` (Milestones 13/14).
- Exceptions: `try/except` handles explicit `raise`; VM runtime errors are mapped to coarse exception types (`RuntimeError`, `TypeError`, `IndexError`, `KeyError`, `ZeroDivisionError`, `NameError`, `AttributeError`) based on message heuristics; tracebacks include filename/line/column and frame names; exception chaining metadata is wired for `raise ... from ...` and implicit context (`__cause__`, `__context__`, `__suppress_context__`); `except` handlers accept user-defined exception classes (including subclass and tuple matching) via runtime exception parent tracking; exception subclasses without explicit `__init__` now accept positional args and populate `.args`; `except*` now performs subgroup splitting, multi-handler subgroup delivery, and unmatched-remainder reraising.
- Identity: `id()` builtin returns stable ids; `is`/`is not` are identity-based (heap objects carry stable ids).
- Classes: class bodies execute in a class namespace module while resolving missing names against the defining module; methods capture the defining module as globals; C3 MRO is computed at class creation and exposed via `__mro__`; class-header `metaclass=` is compiled and propagated through VM class-frame creation; class objects retain resolved metaclass metadata for `isinstance(Class, Meta)` behavior and metaclass conflict checks.
- CPython bytecode: marshal reader + `.pyc` loader/writer, decoder, and translator covering a core opcode subset (`RESUME`, `LOAD_CONST`, `LOAD_SMALL_INT`, `LOAD_NAME`, `LOAD_LOCALS`, `LOAD_GLOBAL`, `LOAD_FAST*`, `LOAD_DEREF`, `LOAD_CLOSURE`, `STORE_DEREF`, `LOAD_ATTR` with encoded null flag, `STORE_*`, `BINARY_OP*`, `COMPARE_OP*`, `CONTAINS_OP*`, `IS_OP`, `CALL`/`CALL_KW` + specialized call forms, `MAKE_FUNCTION`, `SET_FUNCTION_ATTRIBUTE`, `LOAD_BUILD_CLASS`, `PUSH_NULL`, `GET_ITER`/`FOR_ITER`, `SEND`, `YIELD_VALUE`/`YIELD_FROM`, jumps, `RETURN_*`, `IMPORT_NAME`/`IMPORT_FROM`). Marshal float constants are supported (`g` binary float), and `BINARY_OP` true-division args map to VM `BinaryDiv`. Unsupported opcodes fail translation explicitly; decode/translation run jump-target and stack-shape validation.
- Modules: new `Value::Module` with per-module globals; VM maintains module cache and search paths (default CWD, configurable via `Vm::add_module_path`/`Vm::add_module_path_front`, and synchronized with `sys.path`). `sys` foundation module is bootstrapped (`path`, `meta_path`, `path_hooks`, `path_importer_cache`, `modules`), with `meta_path` defaulting to `pyrs.PathFinder` and `path_hooks` defaulting to `pyrs.FileFinder`. Imports resolve through finder/loader contracts: meta path finder -> path hooks/importer cache -> loader create/exec (`pyrs.SourceFileLoader` / `pyrs.SourcelessFileLoader` / `pyrs.NamespaceLoader`). Import loads `<name>.py`, sourceless `.pyc` fallbacks (module/package `__init__`), or package `__init__.py` into a module frame, returning module objects; filesystem namespace packages (directories without `__init__.py`) are supported with aggregated `__path__` across module roots; submodule resolution consults loaded parent-package `__path__` before global `sys.path`; module attribute access attempts to lazy-load submodules; source `from .` relative imports resolve via package context; module metadata/spec (`__name__`, `__package__`, `__spec__`, `__loader__`, `__file__`, `__path__`) is populated for supported loader scenarios; functions capture defining module globals.
- Numeric compatibility: `bool` participates in int arithmetic/comparisons (`True == 1`, `True + 1`, etc.), float values are first-class (`float()` builtin + literal parsing), and mixed int/bool/float arithmetic/comparisons are wired; `Value::BigInt` now covers core large-int arithmetic/bitwise/shift/comparison paths (`+`, `-`, `*`, `**`, `~`, `&`, `|`, `^`, `<<`, `>>`, ordering), Python-floor `//`/`%`/`divmod` semantics, large `int(...)` parsing, and `%x`/`%X`/`%o` formatting, with lazy bigint-backed `range` iterator fallback for large stops.
- Stdlib bootstrap additions: builtin modules for `math`, `time`, `os`, `pathlib`, `json`, `_json` (accelerator-presence shim), `_csv` (dialect registry plus hardened reader/writer options: `skipinitialspace`, `quotechar=None`, `escapechar`, `quoting`, and `field_size_limit` enforcement), `codecs`, `re`, `operator`, `itertools`, `functools`, `collections`, `types`, `inspect`, `io`, `datetime`, `asyncio`, `threading`, `signal`, `_socket`, `uuid`, `dataclasses`, and `__future__` are preinstalled with foundational callable coverage; builtin `random` module remains preinstalled (`seed`, `random`, `randrange`, `randint`, `getrandbits`, `choice`, `shuffle`) with deterministic reseed behavior; builtin `round` now supports banker's rounding semantics (`ndigits`, negative precision, float/int/bigint paths, and `__round__` dispatch). `json.dumps` now supports baseline kwargs (`sort_keys`, `separators`, `ensure_ascii`, `allow_nan`, `default`) and `json.loads` accepts UTF-8 `str`/`bytes`/`bytearray`; `re.match`/`search`/`fullmatch` now return match objects with baseline `group`/`groups`/`start`/`end`/`span` semantics used by stdlib call paths (`argparse`, `pickletools`) plus baseline `Pattern.finditer`; codec foundations now include `utf-32` plus `raw-unicode-escape`/`unicode-escape` encode/decode paths and `codecs.escape_decode`; `_struct` format call sites now accept bytes-like format arguments used by stdlib `zipfile`; `os`/`posix` now provide non-`NoOp` `open`/`close`/`isatty`/`stat`/`lstat`/`rmdir`/`utime`/`scandir` and wait-status helper callables; `math` now provides non-`NoOp` implementations for core transcendentals/aggregates (`ldexp`, `hypot`, `fabs`, `exp`, `erfc`, `log`, `fsum`, `sumprod`, trig family, and `isclose`); `operator`/`functools` now provide non-`NoOp` callable adapters for `itemgetter`/`attrgetter`/`methodcaller` and `cmp_to_key` with `sorted`/`min`/`max` key interoperability, `functools.partial` unwraps `staticmethod(...)`/`classmethod(...)` wrappers for class-body partial/partialmethod compatibility, `functools.wraps` propagates wrapper metadata for function/bound-method inputs, and `functools.cached_property` now executes descriptor-backed cache semantics; `itertools` now provides non-`NoOp` implementations for the prior long-tail stub set (`accumulate`, `combinations*`, `compress`, `dropwhile`, `filterfalse`, `groupby`, `islice`, `pairwise`, `starmap`, `takewhile`, `tee`, `zip_longest`); `inspect.signature` now executes a non-`NoOp` path returning a baseline `Signature` instance with parameter metadata; `importlib.invalidate_caches` plus baseline `importlib.util.spec_from_file_location` now execute non-`NoOp` paths; `_frozen_importlib.spec_from_loader`/`_verbose_message` and `_frozen_importlib_external` `_path_*`/`_unpack_uint*` now execute non-`NoOp` paths; `_opcode` metadata helpers (`stack_effect`, `has_*`, `get_executor`) now execute non-`NoOp` metadata-backed paths; `decimal.getcontext`/`setcontext`/`localcontext`, `_thread.start_new_thread`, and `_warnings._acquire_lock`/`_release_lock` now execute non-`NoOp` baseline paths; `threading` foundations now include `local` plus class methods for `Thread`/`Event`/`Condition`/`Semaphore`/`BoundedSemaphore`/`Barrier`; `_socket` class methods `socket.__init__`/`close`/`detach`/`fileno` plus module-level helpers now execute non-`NoOp` baseline paths; `uuid` foundations (`UUID.__init__`, `uuid1/3/4/5/6/7/8`, `getnode`, namespace constants) now execute non-`NoOp` baseline paths; baseline `dataclasses` helpers (`field`, `is_dataclass`, `fields`, `asdict`, `astuple`, `replace`, `make_dataclass`) execute non-`NoOp` paths including `make_dataclass(..., module=...)`; `datetime` now exports baseline `date` and `timedelta` symbols for stdlib imports with `date.__init__(year, month, day)` constructor support; `float.fromhex`/`float.hex`, `str.maketrans`, `str.find`, `str.rsplit`, and `str.isalnum` now execute non-`NoOp` helper logic; list/dict helper coverage includes `list.reverse` and mapping/iterable-of-pairs `dict.update`; `bytes.join` now supports iterable bytes-like payloads; `object.__reduce_ex__` now executes a non-`NoOp` baseline path with builtin-payload reconstruction for core containers, includes builtin singleton name reductions for `Ellipsis`/`NotImplemented`, caches resolved `copyreg` helper callables in-VM to remove repeated import/getattr overhead, and builtin callable `__name__` resolution plus bound `__reduce_ex__` dispatch now support `pickle.dumps(len)`-style global roundtrips; `_pylong` conversion/division helpers (`int_to_decimal_string`, `int_divmod`, `int_from_string`, `compute_powers`, `_dec_str_to_int_inner`) now execute non-`NoOp` baseline paths; `sys.exit`, builtins `filter`/generic-iterator `enumerate`, `ord`/`chr`, `_colorize.decolor`, list slice assignment semantics, and `platform.win32_is_iot`, `platform.libc_ver`, `binascii.crc32`, `collections._count_elements`, and `atexit` register/unregister/run/clear helpers now execute non-`NoOp` paths.
- Platform shim note: `platform.libc_ver()` now returns a `(lib, version)` tuple (detects `musl`/`glibc` at build target level, otherwise empty defaults) to satisfy CPython harness callers.
- Local import shims live under `shims/` (`shims/enum.py`, `shims/pkgutil.py`, `shims/importlib/resources.py`, `shims/_pickle.py`, `shims/copyreg.py`) and are loaded only from that directory; `enum` is force-shimmed to avoid stdlib shadowing regressions, `pkgutil`/`importlib.resources` are fallback-only for stdlib-less environments and now return payload bytes/text (not file-handle objects) on `get_data`/`read_*` paths, `_pickle` exposes a compatibility wrapper surface (`PickleBuffer`, `dump`/`dumps`/`load`/`loads`, `Pickler`/`Unpickler`) while delegating to pure-Python `pickle` behavior, and `copyreg` exposes baseline reducer/extension registries for stdlib-less pickle paths.
- Stdlib implementation direction: prefer CPython official pure-Python stdlib modules whenever feasible; keep VM-native module logic only where required and track each remaining native path in `docs/STUB_ACCOUNTING.md`. `json` pure-module preference wiring now exists as an explicit VM opt-in (`Vm::enable_pure_json_preference`) and remains disabled by default while strict-parity/runtime blockers are closed; native fallback remains default for stability.
- Scoping: `global` and `nonlocal` statements supported inside functions; closures wired via cell/free vars; assignments to globals emit `StoreGlobal`.
- Language-surface caveats tracked for Milestone 13 and later: full async-comprehension parity, remaining `ExceptionGroup` edge/API parity, remaining pattern edge/form parity, and full PEP 701 formatting edge cases remain pending (see roadmap/readiness docs).
- Expanded-probe carry-forward blockers are tracked in `docs/STUB_ACCOUNTING.md` (notably `_testinternalcapi.hamt`).
- `.pyc` header parsing/writing (hash-based + timestamp) + executor (`Vm::execute_pyc_*`), with CLI support for `.pyc` paths.
- Code objects carry `filename` plus per-instruction source locations for tracebacks.
- Tests: parser smoke tests, bytecode metadata loader test, pyc header read/write tests, CPython `.pyc` execution + rewrite roundtrip tests, translation validation tests (jump/stack guards), arithmetic fuzz/property tests, parser/compiler/VM no-panic fuzzing (`tests/fuzz_parser_vm.rs`), differential CPython parity tests (`tests/differential_cpython.rs`, including malformed-input contract probes for `json`, `_csv`, and pickle object-protocol paths), active CPython harness suites (`tests/cpython_harness.rs` + `tests/cpython_suite_language.txt` + `tests/cpython_suite_imports.txt` + `tests/cpython_suite_strict_stdlib.txt`; `tests/cpython_allowlist.txt` is empty and strict stdlib gaps are tracked in `tests/cpython_allowlist_strict.txt`; strict suite entries execute via isolated `pyrs` subprocesses with timeout guard and helper timeout regressions, are opt-in for local frequent runs via `PYRS_RUN_STRICT_STDLIB=1` or `PYRS_PARITY_STRICT=1`, skip allowlisted strict entries by default unless `PYRS_RUN_ALLOWLISTED_STRICT=1`, and support explicit subprocess/perf controls via `PYRS_SUBPROCESS_BIN`, `PYRS_SUBPROCESS_BIN_MODE`, `PYRS_STRICT_ALLOWLIST_TIMEOUT_SECS`, `PYRS_STRICT_TIMING`, and `PYRS_STRICT_UNITTEST_VERBOSITY`), integration package test, import-system tests (`sys.path` mutation, `sys.modules`, `__import__` top-level/fromlist/relative level, namespace packages including multi-root `__path__`, submodule resolution through package `__path__`, `sys.meta_path` and `sys.path_hooks` object/string contract entries, and `sys.path_importer_cache` behavior), float/parser regression tests (float literals including leading-dot form, `/` and `/=` parsing), VM float/random regression tests (mixed numeric semantics + deterministic `random.seed(...)` flows), Milestone 9 regressions (set/bytes/memoryview/complex builtins, `iter`/`next`, metaclass keyword handling, `__slots__`, `codecs`, `os`/`posix` open/stat/wait helpers, and extended `math` behavior), Milestone 10 async/concurrency regressions (`asyncio.run`, `await`, `async for`, `async with`, async generators/`anext`, async inspect predicates, threading/signal foundations), Milestone 13 regressions for pattern binding/reachability validation, future-annotation deferral/`__future__` import behavior, dataclass keyword-only decorator flows, module `__getattr__` fallback, `dict.copy`, hash-index stability under heavy `dict.pop(...)` mutation, csv reader/writer option handling (`skipinitialspace`, `escapechar`, `quotechar=None`, `quoting`, field limits, EOF escape handling, and dialect-override semantics), threading class methods, socket object methods, uuid foundations, `object.__reduce_ex__`, bigint arithmetic/bitwise/shift/comparison parity, lazy large-stop `range` semantics, `round` builtin banker's-rounding regressions (`tests/vm.rs`), `with/assertRaises` attribute-error unwind regression coverage (no `PopTop` underflow), and `functools.cached_property`/stdlib `ipaddress` import paths (`tests/vm.rs`, with the ipaddress regression probe running in a dedicated larger-stack test thread), stub inventory drift gate (`tests/noop_inventory.rs` + `docs/NOOP_BUILTIN_INVENTORY.txt`), module-local unit tests in `src/vm/containers.rs`, `src/vm/ops.rs`, `src/runtime/mod.rs`, `src/vm/stdlib/json.rs`, `src/vm/stdlib/re.rs`, `src/vm/stdlib/csv.rs`, and `src/vm/stdlib/pickle.rs`, runtime GC regressions for self-referential ref-holder objects plus repeated stdlib import/exec bounded-growth checks (`tests/gc_regression.rs`), curated sandboxed smoke tests (`tests/realworld_smoke.rs`; parity profile `scripts/run_parity_gate.sh`), and baseline CI parity workflow (`.github/workflows/parity-gate.yml`).
- CLI: `--ast`, `--bytecode`, `-S`/`--no-site`, and `.pyc` execution by file extension; startup performs baseline `site` import when stdlib paths are discoverable (strict failure when `PYRS_CPYTHON_LIB` is explicitly configured).
