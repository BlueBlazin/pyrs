# Project Context: Python Interpreter in Rust

## Vision
Build a production‑grade Python interpreter in Rust with **full source and bytecode compatibility** with **CPython 3.14**, designed for long‑term evolution (JIT and extension support possible later), and minimal third‑party dependencies.

## Target Compatibility
- **Python version:** CPython **3.14**
- **Compatibility goals (now):**
  - Run Python source code.
  - Execute CPython 3.14 bytecode.
- **Non‑goals (now):**
  - JIT compilation.
  - Full CPython C‑API compatibility / C‑extensions.
- **Future‑friendly design:**
  - Keep architecture flexible so **JIT** and **extension support** can be added later.
  - Expect that popular packages (e.g., NumPy) typically need C‑extension support; plan for this later.

## Parsing
- Use CPython 3.14 grammar with a **packrat parser**.
- Grammar reference: https://docs.python.org/3/reference/grammar.html
- **Minimize dependencies**: prefer small, well‑scoped crates or bespoke implementations.

## Compiler & Execution Pipeline
- **AST → Bytecode IR** (CPython‑compatible bytecode).
- Bytecode IR should support **light optimizations** while preserving semantics.
- Design IR with a path to **JIT** in the future.

## Runtime Model
- Match CPython object model.
- **Reference‑counted GC** similar to CPython.
- Object identity uses stable `id()` values; `is`/`is not` are identity-based with a basic cycle GC for self-referential containers.
- **GIL** (single‑threaded execution model for now).
- GIL‑free execution is **not** a current goal.

## Standard Library
- Start with a **practical subset** of stdlib.
- Expand toward full coverage over time.

## Success Criteria
We measure success by:
- Running **real‑world apps**.
- Passing **CPython test suites** (as compatible as possible).
- Competitive **performance benchmarks**.

## Milestone Sketch (Iterative)
Roadmap milestones are now maintained in `docs/ROADMAP.md` and are structured to guarantee full CPython 3.14 parity with no "basic compat" completion criteria.

Current milestone state summary:
1. Milestones 0-3 complete (foundations: parser/AST, runtime identity+GC, bytecode intake, closures+frames).
2. Milestone 4 complete (P0): lazy generator suspension/resume + `yield from` send/throw delegation + `StopIteration` return-value propagation + close/`GeneratorExit` handling + reentrancy guard coverage.
3. Milestone 5 complete (P0): supported-path opcode hardening (explicit unsupported-opcode errors + decode/translation jump/stack validation) and supported-subset `.pyc` writer (header + marshal).
4. Milestone 6 complete (P0): import-system parity delivered for supported pure-Python scenarios (relative imports, namespace packages, `sys.path`/`sys.meta_path`/`sys.path_hooks`/`sys.path_importer_cache` contracts, module metadata/spec fields, `importlib` helper APIs).
5. Milestone 7 complete (P0): language-surface milestone delivered (decorators, assignment expressions, list/dict comprehensions + generator expressions with scope isolation, core `match`/`case` subset, async syntax lowering, `except*` parsing, type parameters on `def`/`class`, f-string lowering, and `__future__` placement/unknown-feature compile checks).
6. Milestone 8 complete (P0): runtime data-model semantics delivered for descriptor hooks, attribute-model hook paths, C3 MRO/`super(type, obj)`, and exception chaining metadata.
7. Milestone 9 complete (P0/P1): core runtime types (`set`/`frozenset`, `bytes`/`bytearray`/`memoryview`, `complex`), class metaclass keyword path + `__slots__` core behavior, and stdlib bootstrap foundations (including `codecs` and previously landed `random`) are implemented.
8. Milestone 10 complete (P1): core async/concurrency semantics are implemented for milestone scope (`async`/`await`, async generators, `aiter`/`anext`, foundational `asyncio`/`threading`/`signal` support).
9. Milestone 11 complete (P0/P1): parity/test gate foundations are active (CPython harness suites + differential CPython checks + parser/VM fuzzing + curated real-world smoke suite + parity profile script).
10. Milestone 12 complete (P0): current curated CPython harness language/import suites pass with an empty allowlist.
11. Remaining milestones 13-16 cover: long-tail language/runtime parity + stdlib/packaging usability closure, performance/observability/runtime hooks, native-extension ecosystem compatibility, and release hardening/certification.

## Engineering Principles
- Maintain correctness first, then performance.
- Keep dependencies minimal and well‑justified.
- Prefer clear architecture boundaries: parser, AST, compiler/IR, VM, runtime, stdlib.

## Project Artifacts
- Roadmap: `docs/ROADMAP.md`
- Milestone 12 completion report: `docs/MILESTONE_12_BACKLOG.md`
- Compatibility tracker: `docs/COMPATIBILITY.md`
- Production readiness accounting: `docs/PRODUCTION_READINESS.md`
- Stub and partial implementation ledger: `docs/STUB_ACCOUNTING.md`
- Generated `NoOp` inventory: `docs/NOOP_BUILTIN_INVENTORY.txt` (refresh with `cargo run --quiet --bin print_noop_inventory > docs/NOOP_BUILTIN_INVENTORY.txt`)
- `NoOp` inventory traversal is recursive across module/class/instance/container graphs.
- Current `NoOp` inventory is reduced to test-only `_testcapi`/`_testinternalcapi` stubs plus `sys.monitoring` and `sys._jit` scaffolding.
- CPython vendor sync script: `scripts/sync_cpython.py`
- Vendor snapshot: CPython 3.14.3 grammar + opcode sources synced into `vendor/cpython-3.14/` (opcode table CSV generated).

## Current Scaffolding (Early Stage)
- Parser: packrat-style memoization with indentation tokens, explicit backslash line-join handling, Unicode identifiers, and soft-keyword disambiguation (`match`/`case`/`type`); statements include `if`/`elif`/`else`/`while`/`for`, `break`/`continue`, `with` (including multi-item forms), `match`/`case` (literal/capture/guard plus sequence/mapping/class/or/as/star families, class-pattern positional-after-keyword rejection, correct `or`/`as` precedence (`A | B as n` -> `(A | B) as n`), and statement-grade error surfacing for invalid `match ...:` forms), `try` with `except*` parsing, decorators, `def`/`class` (including type parameters on headers and class-header `metaclass=` keyword args), async statement forms (`async def`/`async for`/`async with`), imports, annotation syntax, chained assignment, and `del`. Expressions include assignment expressions (`:=`), list/dict comprehensions + generator expressions, await syntax, `yield`/`yield from`, starred tuple/list displays, implicit adjacent string literal concatenation, and f-string lowering.
- Bytecode: minimal opcodes for source compiler plus CPython 3.14 decoder/translator (`opcode_table.csv` from CPython 3.14).
- Compiler/VM: emits and executes bytecode for prior core feature set plus Milestone 7/10 additions: decorator application lowering, assignment expressions (`:=`), list/dict comprehensions and generator expressions via isolated synthetic function scopes, core `match`/`case` lowering (literal/capture/guard plus compile-time validation for duplicate capture names, OR-alternative binding-set parity, and irrefutable-pattern reachability rules), async statement/expression runtime semantics (`async def`/`await`/`async for`/`async with`, `GET_AWAITABLE`, coroutine/async-generator protocol hooks, `aiter`/`anext`), f-string lowering to concatenation + `str(...)` conversion, chained assignment lowering (single RHS evaluation + multi-target stores), generalized subscript assignment targets (non-name container expressions), and delete opcodes (`DeleteName`/`DeleteAttr`/`DeleteSubscript`). Arithmetic now includes true division (`/`) and float-aware semantics for `/`, `//`, `%`, `**`, and unary `+`/`-`. Builtin `exec` now executes source strings and code objects with explicit globals/locals namespace handling (including closure arity/type validation for code-object exec). `__future__` compile checks enforce top-of-file placement and unknown-feature rejection.
- VM decomposition progress: arithmetic/comparison/type-union operator kernels have been extracted into `src/vm/ops.rs` (no behavior change), reducing pressure on `src/vm/mod.rs` and preparing cleaner bigint integration work.
- Runtime data model (Milestones 8-9 + 13 progress): descriptor-aware attribute load/store paths (`__get__`, `__set__`, `__delete__`), instance hooks (`__getattribute__` override baseline plus `__getattr__`, `__setattr__`, `__delattr__`), C3 MRO metadata (`__mro__`, `__bases__`), explicit-args `super(type, obj)` lookup object, class-header `metaclass=` path, `__build_class__` metaclass kwarg handling, metaclass conflict detection/selection across bases, class-object metaclass fallback attribute lookup, `__slots__` restrictions (including empty slots and `__dict__` slot behavior), and builtin attr helpers (`getattr`, `setattr`, `delattr`, `hasattr`, `object.__getattribute__`).
- Container-internals caveat (P0/P2 carry-forward): runtime `dict`/`set`/`frozenset` now use hash-indexed runtime container objects with insertion-order backing vectors. Core unhashable key/item rejection is enforced on constructor/update/assignment/membership flows; dict equality is insertion-order independent and set/frozenset equality is value-based (including cross-type equality). Remaining CPython hash/equality edge semantics and hot-path performance closure are tracked in `docs/STUB_ACCOUNTING.md`, `docs/PRODUCTION_READINESS.md`, and `docs/ROADMAP.md` (Milestones 13/14).
- Exceptions: `try/except` handles explicit `raise`; VM runtime errors are mapped to coarse exception types (`RuntimeError`, `TypeError`, `IndexError`, `KeyError`, `ZeroDivisionError`, `NameError`, `AttributeError`) based on message heuristics; tracebacks include filename/line/column and frame names; exception chaining metadata is wired for `raise ... from ...` and implicit context (`__cause__`, `__context__`, `__suppress_context__`); `except` handlers accept user-defined exception classes (including subclass and tuple matching) via runtime exception parent tracking; `except*` now performs subgroup splitting, multi-handler subgroup delivery, and unmatched-remainder reraising.
- Identity: `id()` builtin returns stable ids; `is`/`is not` are identity-based (heap objects carry stable ids).
- Classes: class bodies execute in a class namespace module while resolving missing names against the defining module; methods capture the defining module as globals; C3 MRO is computed at class creation and exposed via `__mro__`; class-header `metaclass=` is compiled and propagated through VM class-frame creation; class objects retain resolved metaclass metadata for `isinstance(Class, Meta)` behavior and metaclass conflict checks.
- CPython bytecode: marshal reader + `.pyc` loader/writer, decoder, and translator covering a core opcode subset (`RESUME`, `LOAD_CONST`, `LOAD_SMALL_INT`, `LOAD_NAME`, `LOAD_LOCALS`, `LOAD_GLOBAL`, `LOAD_FAST*`, `LOAD_DEREF`, `LOAD_CLOSURE`, `STORE_DEREF`, `LOAD_ATTR` with encoded null flag, `STORE_*`, `BINARY_OP*`, `COMPARE_OP*`, `CONTAINS_OP*`, `IS_OP`, `CALL`/`CALL_KW` + specialized call forms, `MAKE_FUNCTION`, `SET_FUNCTION_ATTRIBUTE`, `LOAD_BUILD_CLASS`, `PUSH_NULL`, `GET_ITER`/`FOR_ITER`, `SEND`, `YIELD_VALUE`/`YIELD_FROM`, jumps, `RETURN_*`, `IMPORT_NAME`/`IMPORT_FROM`). Marshal float constants are supported (`g` binary float), and `BINARY_OP` true-division args map to VM `BinaryDiv`. Unsupported opcodes fail translation explicitly; decode/translation run jump-target and stack-shape validation.
- Modules: new `Value::Module` with per-module globals; VM maintains module cache and search paths (default CWD, configurable via `Vm::add_module_path`/`Vm::add_module_path_front`, and synchronized with `sys.path`). `sys` foundation module is bootstrapped (`path`, `meta_path`, `path_hooks`, `path_importer_cache`, `modules`), with `meta_path` defaulting to `pyrs.PathFinder` and `path_hooks` defaulting to `pyrs.FileFinder`. Imports resolve through finder/loader contracts: meta path finder -> path hooks/importer cache -> loader create/exec (`pyrs.SourceFileLoader` / `pyrs.SourcelessFileLoader` / `pyrs.NamespaceLoader`). Import loads `<name>.py`, sourceless `.pyc` fallbacks (module/package `__init__`), or package `__init__.py` into a module frame, returning module objects; filesystem namespace packages (directories without `__init__.py`) are supported with aggregated `__path__` across module roots; submodule resolution consults loaded parent-package `__path__` before global `sys.path`; module attribute access attempts to lazy-load submodules; source `from .` relative imports resolve via package context; module metadata/spec (`__name__`, `__package__`, `__spec__`, `__loader__`, `__file__`, `__path__`) is populated for supported loader scenarios; functions capture defining module globals.
- Numeric compatibility: `bool` participates in int arithmetic/comparisons (`True == 1`, `True + 1`, etc.), float values are first-class (`float()` builtin + literal parsing), and mixed int/bool/float arithmetic/comparisons are wired; `Value::BigInt` now covers core large-int arithmetic/bitwise/shift/comparison paths (`+`, `-`, `*`, `**`, `~`, `&`, `|`, `^`, `<<`, `>>`, ordering) with lazy bigint-backed `range` iterator fallback for large stops.
- Stdlib bootstrap additions: builtin modules for `math`, `time`, `os`, `pathlib`, `json`, `codecs`, `re`, `operator`, `itertools`, `functools`, `collections`, `types`, `inspect`, `io`, `datetime`, `asyncio`, `threading`, `signal`, `_socket`, `uuid`, and `dataclasses` are preinstalled with foundational callable coverage; builtin `random` module remains preinstalled (`seed`, `random`, `randrange`, `randint`, `getrandbits`, `choice`, `shuffle`) with deterministic reseed behavior; codec foundations now include `utf-32` encode/decode paths; `os`/`posix` now provide non-`NoOp` `open`/`close`/`isatty`/`stat`/`lstat`/`rmdir`/`utime`/`scandir` and wait-status helper callables; `math` now provides non-`NoOp` implementations for core transcendentals/aggregates (`ldexp`, `hypot`, `fabs`, `exp`, `erfc`, `log`, `fsum`, `sumprod`, trig family, and `isclose`); `operator`/`functools` now provide non-`NoOp` callable adapters for `itemgetter`/`attrgetter`/`methodcaller` and `cmp_to_key` with `sorted`/`min`/`max` key interoperability, `functools.partial` unwraps `staticmethod(...)`/`classmethod(...)` wrappers for class-body partial/partialmethod compatibility, `functools.wraps` propagates wrapper metadata for function/bound-method inputs, and `functools.cached_property` now executes descriptor-backed cache semantics; `itertools` now provides non-`NoOp` implementations for the prior long-tail stub set (`accumulate`, `combinations*`, `compress`, `dropwhile`, `filterfalse`, `groupby`, `islice`, `pairwise`, `starmap`, `takewhile`, `tee`, `zip_longest`); `inspect.signature` now executes a non-`NoOp` path returning a baseline `Signature` instance with parameter metadata; `importlib.invalidate_caches` plus baseline `importlib.util.spec_from_file_location` now execute non-`NoOp` paths; `_frozen_importlib.spec_from_loader`/`_verbose_message` and `_frozen_importlib_external` `_path_*`/`_unpack_uint*` now execute non-`NoOp` paths; `_opcode` metadata helpers (`stack_effect`, `has_*`, `get_executor`) now execute non-`NoOp` metadata-backed paths; `decimal.getcontext`/`setcontext`/`localcontext`, `_thread.start_new_thread`, and `_warnings._acquire_lock`/`_release_lock` now execute non-`NoOp` baseline paths; `threading` foundations now include `local` plus class methods for `Thread`/`Event`/`Condition`/`Semaphore`/`BoundedSemaphore`/`Barrier`; `_socket` class methods `socket.__init__`/`close`/`detach`/`fileno` plus module-level helpers now execute non-`NoOp` baseline paths; `uuid` foundations (`UUID.__init__`, `uuid1/3/4/5/6/7/8`, `getnode`, namespace constants) now execute non-`NoOp` baseline paths; baseline `dataclasses` helpers (`field`, `is_dataclass`, `fields`, `asdict`, `astuple`, `replace`, `make_dataclass`) execute non-`NoOp` paths; `float.fromhex`/`float.hex`, `str.maketrans`, `str.find`, `str.rsplit`, and `str.isalnum` now execute non-`NoOp` helper logic; list/dict helper coverage includes `list.reverse` and mapping/iterable-of-pairs `dict.update`; `object.__reduce_ex__` now executes a non-`NoOp` baseline path; `_pylong` conversion/division helpers (`int_to_decimal_string`, `int_divmod`, `int_from_string`, `compute_powers`, `_dec_str_to_int_inner`) now execute non-`NoOp` baseline paths; `sys.exit`, builtins `filter`/generic-iterator `enumerate`, `_colorize.decolor`, list slice assignment semantics, and `platform.win32_is_iot`, `platform.libc_ver`, `binascii.crc32`, `collections._count_elements`, and `atexit` register/unregister/run/clear helpers now execute non-`NoOp` paths.
- Platform shim note: `platform.libc_ver()` now returns a `(lib, version)` tuple (detects `musl`/`glibc` at build target level, otherwise empty defaults) to satisfy CPython harness callers.
- Local import shims live under `shims/` (`shims/enum.py`, `shims/pkgutil.py`, `shims/importlib/resources.py`) and are loaded only from that directory; `enum` is force-shimmed to avoid stdlib shadowing regressions, while `pkgutil`/`importlib.resources` are fallback-only for stdlib-less environments.
- Scoping: `global` and `nonlocal` statements supported inside functions; closures wired via cell/free vars; assignments to globals emit `StoreGlobal`.
- Language-surface caveats tracked for Milestone 13 and later: full async-comprehension parity, remaining `ExceptionGroup` edge/API parity, remaining pattern edge/form parity, and full PEP 701 formatting edge cases remain pending (see roadmap/readiness docs).
- Expanded-probe carry-forward blockers are tracked in `docs/STUB_ACCOUNTING.md` (notably class-statement inheritance hangs for `seq_tests`-style bases and `_testinternalcapi.hamt`).
- `.pyc` header parsing/writing (hash-based + timestamp) + executor (`Vm::execute_pyc_*`), with CLI support for `.pyc` paths.
- Code objects carry `filename` plus per-instruction source locations for tracebacks.
- Tests: parser smoke tests, bytecode metadata loader test, pyc header read/write tests, CPython `.pyc` execution + rewrite roundtrip tests, translation validation tests (jump/stack guards), arithmetic fuzz/property tests, parser/compiler/VM no-panic fuzzing (`tests/fuzz_parser_vm.rs`), differential CPython parity tests (`tests/differential_cpython.rs`), active CPython harness suites (`tests/cpython_harness.rs` + `tests/cpython_suite_language.txt` + `tests/cpython_suite_imports.txt`, with empty `tests/cpython_allowlist.txt`), integration package test, import-system tests (`sys.path` mutation, `sys.modules`, `__import__` top-level/fromlist/relative level, namespace packages including multi-root `__path__`, submodule resolution through package `__path__`, `sys.meta_path` and `sys.path_hooks` object/string contract entries, and `sys.path_importer_cache` behavior), float/parser regression tests (float literals including leading-dot form, `/` and `/=` parsing), VM float/random regression tests (mixed numeric semantics + deterministic `random.seed(...)` flows), Milestone 9 regressions (set/bytes/memoryview/complex builtins, `iter`/`next`, metaclass keyword handling, `__slots__`, `codecs`, `os`/`posix` open/stat/wait helpers, and extended `math` behavior), Milestone 10 async/concurrency regressions (`asyncio.run`, `await`, `async for`, `async with`, async generators/`anext`, async inspect predicates, threading/signal foundations), Milestone 13 regressions for pattern binding/reachability validation plus threading class methods, socket object methods, uuid foundations, `object.__reduce_ex__`, bigint arithmetic/bitwise/shift/comparison parity, lazy large-stop `range` semantics, and `functools.cached_property`/stdlib `ipaddress` import paths (`tests/vm.rs`), stub inventory drift gate (`tests/noop_inventory.rs` + `docs/NOOP_BUILTIN_INVENTORY.txt`), curated sandboxed smoke tests (`tests/realworld_smoke.rs`; parity profile `scripts/run_parity_gate.sh`), and baseline CI parity workflow (`.github/workflows/parity-gate.yml`).
- CLI: `--ast`, `--bytecode`, `-S`/`--no-site`, and `.pyc` execution by file extension; startup performs baseline `site` import when stdlib paths are discoverable (strict failure when `PYRS_CPYTHON_LIB` is explicitly configured).
