# CPython C-API Plan (ABI-First, NumPy-Focused)

Status: active (Milestone 15 execution strategy).

## Execution Lock (Do Not Derail)

This is the active workstream. If context is compacted, resume from this section first.

- [ ] P0 lifetime-model closure is active (`docs/CAPI_LIFETIME_MODEL.md`):
  - no per-context frees for escaped/shared CPython wrappers,
  - ownership must move to VM-global registry + explicit borrowed/new/stolen semantics,
  - temporary pinning is crash containment only and must be retired after registry migration.
- [x] Baseline lifetime hardening lane landed in CI (`sanitizer-stability` ASan job in `.github/workflows/parity-gate.yml`).
- [x] Two-lane strategy + acceptance criteria documented (`docs/CAPI_PLAN.md`).
- [x] CPython 3.14 abi3 manifest generation is in place (`scripts/generate_abi3_manifest.py`, `perf/abi3_manifest_latest.json`).
- [x] Conformance tests expanded with behavior-level abi3 semantics across batch probes (`tests/extension_smoke.rs`).
- [x] NumPy gate is mapped as P0 Lane B (`docs/NUMPY_BRINGUP_GATE.md`).
- [ ] Checkpoint discipline: land work in small commits per batch (`manifest` -> `tests` -> `ABI impl` -> `NumPy gate refresh`).
- [x] Root-cause fix for module-state/attr publication in NumPy import path is landed (`numpy_import` now passes in direct mode).
- [x] `numpy_ndarray_sum` closure landed: `int(np.array([1,2,3]).sum()) == 6` now passes in direct mode.
- [x] Deterministic `numpy._core.numerictypes` completion gate is landed (`numpy_numerictypes_core`: `int8`/`float64`/`bool_`).

## ABI3 Status Snapshot (2026-02-16)

Source: `perf/abi3_manifest_latest.json` (generated by `scripts/generate_abi3_manifest.py`).

- Stable ABI function exports implemented: `782 / 782`
- Stable ABI data exports implemented: `143 / 143`

Conclusion: Lane A abi3 symbol closure is complete for CPython 3.14 Stable ABI surface (`782/782`, `143/143`). Remaining P0 focus is Lane B scientific-stack non-abi3 closure.

## Goal

Provide a production-grade native extension substrate in `pyrs` that:

1. Implements CPython 3.14 Stable ABI (`abi3`) rigorously.
2. Supports NumPy/scientific stack native execution without CPython runtime fallback.

## Why Not “Implement All of Python.h”

`Python.h` is too large and includes non-stable/private/internal surfaces that do not define a bounded release target.  
The practical release target is:

- **Lane A**: full CPython 3.14 Stable ABI (`Misc/stable_abi.toml`).
- **Lane B**: explicit non-abi3 surfaces required by real target extensions (NumPy first).

This keeps scope finite and testable while still unblocking scientific stack support.

## Critical Constraint

Stable ABI completion alone is **necessary but not sufficient** for NumPy/scientific stack:

- Local NumPy wheel here is `cp314-cp314` (not `abi3`):  
  `/Users/$USER/pyrs/.venv-ext314/lib/python3.14/site-packages/numpy-2.4.2.dist-info/WHEEL`

Therefore, Lane B remains required.

## Two-Lane Execution Model

### Lane A (P0): Stable ABI (`abi3`) closure

Source of truth:
- CPython 3.14 `Misc/stable_abi.toml`

Required outputs:
- Machine-generated manifest of Stable ABI symbols and current `pyrs` export coverage:
  - `perf/abi3_manifest_latest.json`
- Coverage script:
  - `scripts/generate_abi3_manifest.py`
- ABI conformance tests (symbol presence + semantics for implemented slices).

Acceptance criteria:
1. `function` and `data` coverage in manifest reaches 100%.
2. Stable ABI behavior tests are green in CI.
3. No untracked temporary compatibility patches in C-API core paths.

### Lane B (P0): NumPy/scientific-stack non-abi3 closure

Source of truth:
- Real extension artifacts (wheel/source), dynamic-link symbol usage, runtime call traces.

Required outputs:
- Explicit gap list in `docs/NUMPY_BRINGUP_GATE.md` mapped by subsystem.
- Deterministic gate progression in:
  - `perf/numpy_gate_direct_latest.json`
- Subsystem-first fixes (object/type model, call protocol, error semantics, module init), no attr-by-attr patch churn.

Acceptance criteria:
1. `import numpy` passes in direct mode.
2. Baseline ndarray smoke passes (`int(np.array([1,2,3]).sum()) == 6`).
3. Scientific stack import/smoke cases pass per gate policy.

## Engineering Rules for This Plan

1. No trial-and-error symbol patch churn.
2. Fix root semantics in shared substrate first.
3. Every temporary workaround must be tracked in:
   - `docs/STUB_ACCOUNTING.md` or
   - `docs/ALGO_AUDIT_BACKLOG.md`
4. Every C-API behavior change must include:
   - targeted test(s),
   - manifest/gate update,
   - docs update in the same checkpoint.

## Current Baseline Snapshot

From `perf/abi3_manifest_latest.json`:
- Stable ABI functions implemented/exported: `782 / 782`
- Stable ABI data symbols implemented/exported: `143 / 143`

Recent Lane A slice:
- Added Stable-ABI exports and semantics for `PyDict_{Clear,Update,Keys,Values,Items,MergeFromSeq2}`.
- Added Stable-ABI exports and semantics for `PyCapsule_{GetName,SetPointer,GetDestructor,SetDestructor}`.
- Added Stable-ABI exports and semantics for `PyByteArray_*` constructor/access/resize/concat APIs.
- Fixed `PyBuffer_Release` to release pin/ref state (was previously a no-op).
- Added Stable-ABI exports and semantics for list APIs:
  - `PyList_{GetItem,SetItem,Insert,GetSlice,SetSlice,Sort,Reverse}`
- Added Stable-ABI exports and semantics for set APIs:
  - `PySet_New`, `PyFrozenSet_New`, `PySet_{Size,Contains,Add,Discard,Clear,Pop}`
- Added Stable-ABI exports and semantics for exception APIs:
  - `PyException_{GetTraceback,GetCause,GetContext,GetArgs,SetArgs,SetCause,SetContext,SetTraceback}`
  - Getter/setter behavior now supports both internal exception objects and synthetic exception instances.
- Added Stable-ABI exports and semantics for GC APIs:
  - `PyGC_{Collect,Enable,Disable,IsEnabled}`
- Added Stable-ABI exports and semantics for float-info APIs:
  - `PyFloat_{GetMax,GetMin,GetInfo}`
- Added Stable-ABI exports and semantics for bytes APIs:
  - `PyBytes_FromObject`, `PyBytes_Concat`, `PyBytes_ConcatAndDel`
- Added Stable-ABI exports for error/reporting APIs:
  - `PyErr_BadArgument`, `PyErr_BadInternalCall`, `PyErr_PrintEx`, `PyErr_Display`, `PyErr_DisplayException`
- Added Stable-ABI exports for C-function introspection/call APIs:
  - `PyCFunction_Call`, `PyCFunction_GetFunction`, `PyCFunction_GetSelf`, `PyCFunction_GetFlags`
- Added Stable-ABI exports for C-function constructors:
  - `PyCFunction_New`, `PyCFunction_NewEx`, `PyCMethod_New`
- Added Stable-ABI exports and semantics for import APIs:
  - `PyImport_GetModuleDict`
- Added Stable-ABI exports and semantics for codec-registry APIs:
  - `PyCodec_{Register,Unregister,KnownEncoding,Encode,Decode,Encoder,Decoder,IncrementalEncoder,IncrementalDecoder,StreamReader,StreamWriter,RegisterError,LookupError,StrictErrors,IgnoreErrors,ReplaceErrors,XMLCharRefReplaceErrors,BackslashReplaceErrors,NameReplaceErrors}`
- Added Stable-ABI exports and semantics for interpreter-state lifecycle APIs:
  - `PyInterpreterState_{New,Clear,Delete}`
- Added Stable-ABI exports and semantics for module-state registry APIs:
  - `PyState_{AddModule,FindModule,RemoveModule}`
- Added Stable-ABI exports and semantics for member-struct APIs:
  - `PyMember_{GetOne,SetOne}`
  - scalar/object read/write, readonly guards, and relative-offset guard paths are smoke-covered in the batch48 extension probe.
- Added Stable-ABI exports and semantics for struct-sequence APIs:
  - `PyStructSequence_{NewType,New,SetItem,GetItem}` + `PyStructSequence_UnnamedField`
  - baseline type/new/set/get behavior is smoke-covered in the batch49 extension probe.
  - remaining CPython hidden-field semantics (`n_in_sequence` vs real fields) are tracked in `docs/STUB_ACCOUNTING.md`.
- Added Stable-ABI exports and baseline semantics for `PyOS_*` APIs:
  - `PyOS_{BeforeFork,AfterFork_Parent,AfterFork_Child,AfterFork,CheckStack,FSPath,InterruptOccurred,double_to_string,getsig,setsig,mystricmp,mystrnicmp,vsnprintf}`
  - baseline behavior is smoke-covered in the batch50 extension probe.
- Added Stable-ABI data-symbol coverage for exception globals:
  - all previously-missing `PyExc_*` symbols are now exported (`batch51`) and alias compatibility for `EnvironmentError`/`IOError`/`WindowsError` is initialized to `PyExc_OSError`.
  - behavior-level smoke for set/occurred/alias semantics is covered in the batch51 extension probe.
- Added Stable-ABI data-symbol coverage for remaining type/global exports (`batch52`):
  - exported all remaining missing data symbols in the current manifest (`Py*Type` iterator/view globals, weakref globals, filesystem/default-encoding globals, `Py_Version`, `_Py_RefTotal`, `_Py_SwappedOp`, `PyOS_InputHook`).
  - data-symbol coverage is now complete (`143 / 143`), with behavior probes for default-fs metadata/version/swapped-op and representative type-symbol availability.
- Added Stable-ABI exports and baseline semantics for `PyThread_*` runtime APIs (`batch53`):
  - `PyThread_init_thread`, `PyThread_start_new_thread`, `PyThread_exit_thread`, `PyThread_{get_thread_ident,get_thread_native_id}`,
    `PyThread_{allocate_lock,free_lock,acquire_lock,acquire_lock_timed,release_lock}`,
    `PyThread_{get_stacksize,set_stacksize,GetInfo}`,
    deprecated TLS APIs `PyThread_{create_key,delete_key,set_key_value,get_key_value,delete_key_value,ReInitTLS}`,
    and TSS APIs `PyThread_tss_{alloc,free,is_created,create,delete,set,get}`.
  - thread API behavior is smoke-covered for lock acquire/release semantics, TLS/TSS set/get lifecycles, stack-size guard behavior, and start-new-thread return contract.
- Added Stable-ABI exports and baseline semantics for `PyType_*` APIs (`batch54`):
  - `PyType_{FromMetaclass,FromModuleAndSpec,FromSpecWithBases,FromSpec,GetName,GetQualName,GetModuleName,GetFullyQualifiedName,GetSlot,GetModule,GetModuleState,GetModuleByDef,GetTypeDataSize,GetBaseByToken,ClearCache,Modified,Freeze}`.
  - type API behavior is smoke-covered for heap-type creation from `PyType_Spec`, module linkage (`GetModule`/`GetModuleByDef`), token slot + token lookup, slot retrieval, type-data size baseline, and freeze/cache-modified call paths.
- Added Stable-ABI exports and baseline semantics for traceback APIs (`batch55`):
  - `PyTraceBack_{Here,Print}`.
  - traceback API behavior is smoke-covered for non-null frame acceptance (`Here`), null-frame guard error path, and file-write rendering path (`Print`).
- Added Stable-ABI exports and baseline semantics for Unicode error helper APIs (`batch56`):
  - `PyUnicodeDecodeError_Create`
  - `PyUnicodeEncodeError_{GetEncoding,GetObject,GetStart,SetStart,GetEnd,SetEnd,GetReason,SetReason}`
  - `PyUnicodeDecodeError_{GetEncoding,GetObject,GetStart,SetStart,GetEnd,SetEnd,GetReason,SetReason}`
  - `PyUnicodeTranslateError_{GetObject,GetStart,SetStart,GetEnd,SetEnd,GetReason,SetReason}`
  - unicode-error API behavior is smoke-covered for decode-error creation, encode/decode/translate getter+setter semantics, clipping behavior for start/end, and non-`UnicodeError` type guard paths.
- Added Stable-ABI exports and baseline semantics for core Unicode utility APIs (`batch57`):
  - `PyUnicode_{FromObject,FromOrdinal,GetDefaultEncoding,Equal,EqualToUTF8,EqualToUTF8AndSize,ReadChar,Find,FindChar,Count,Join,Split,RSplit,Splitlines,Partition,RPartition,IsIdentifier}`
  - Unicode utility API behavior is smoke-covered for conversion/equality, read/find/count, split/join/partition, and identifier checks.
- Added Stable-ABI exports and baseline semantics for Unicode codec/FS helpers (`batch58`):
  - `PyUnicode_{Decode,DecodeASCII,DecodeLatin1,DecodeUTF8,DecodeUTF8Stateful,DecodeFSDefault,DecodeFSDefaultAndSize,DecodeLocale,DecodeLocaleAndSize,EncodeFSDefault,EncodeLocale,AsDecodedObject,AsDecodedUnicode,AsEncodedObject,AsEncodedUnicode,FSConverter,FSDecoder}`
  - Unicode codec/FS behavior is smoke-covered for decode/encode helper paths, UTF-8 stateful consumed count, FS converter/decoder slot behavior, and decoded/encoded helper type-guard behavior.
- Added Stable-ABI exports and baseline semantics for Unicode append/intern/compare helpers (`batch59`):
  - `PyUnicode_{Append,AppendAndDel,GetSize,InternInPlace,InternImmortal,RichCompare}`
  - Unicode append/intern/compare behavior is smoke-covered for pointer-update append flows, intern no-crash semantics, removed `GetSize` runtime-error path, and rich-compare bool/notimplemented return baselines.
- Added Stable-ABI exports and baseline semantics for Unicode wide-char helpers (`batch60`):
  - `PyUnicode_{FromWideChar,AsWideChar,AsWideCharString,WriteChar}`
  - Unicode wide-char behavior is smoke-covered for fixed-size and NUL-terminated wide decode, required-size probe, owned wide-string allocation, bounded buffer writes, and in-place character mutation/index-error paths.
- Added Stable-ABI exports and baseline semantics for Unicode codec helpers (`batch61`):
  - `PyUnicode_{AsRawUnicodeEscapeString,AsUnicodeEscapeString,AsUTF16String,AsUTF32String,DecodeRawUnicodeEscape,DecodeUnicodeEscape,DecodeUTF16,DecodeUTF16Stateful,DecodeUTF32,DecodeUTF32Stateful}`
  - Unicode codec-helper behavior is smoke-covered for escape round-trips, UTF-16/UTF-32 decode/stateful consumed counts, and byteorder output baselines.
- Added Stable-ABI exports and baseline semantics for core ref/identity/vectorcall helpers (`batch62`):
  - `Py_{Is,IsNone,IsTrue,IsFalse,NewRef,XNewRef,REFCNT,TYPE}` and `PyVectorcall_NARGS`
  - core helper behavior is smoke-covered for identity predicates, refcount-returning helpers, type-pointer extraction, and vectorcall nargs-flag masking.
- Added Stable-ABI exports and baseline semantics for Unicode varargs formatting helpers (`batch63`):
  - `PyUnicode_FromFormatV` (and `PyUnicode_FromFormat` now routes through the same C varargs formatter path).
  - formatting behavior is smoke-covered for direct varargs and `va_list` call forms with integer/string/pointer/percent formatting.
- Added Stable-ABI exports and baseline semantics for weakref helper APIs (`batch64`):
  - `PyWeakref_{NewRef,NewProxy,GetRef,GetObject}`
  - weakref behavior is smoke-covered for creation/get paths, invalid-ref type errors, and callback-callable type guards.
- Added Stable-ABI exports and baseline semantics for runtime-misc APIs (`batch65`):
  - `Py_{AddPendingCall,MakePendingCalls,AtExit,GetRecursionLimit,SetRecursionLimit,GetVersion,GetBuildInfo,GetCompiler,GetPlatform,GetCopyright,ReprEnter,ReprLeave}`
  - runtime-misc behavior is smoke-covered for pending-call execution, recursion-limit mutation, repr recursion guard behavior, and non-empty runtime metadata strings.
- Added Stable-ABI exports and baseline semantics for path-config + locale helper APIs (`batch66`):
  - `Py_{GetArgcArgv,SetProgramName,GetProgramName,SetPythonHome,GetPythonHome,SetPath,GetPath,GetPrefix,GetExecPrefix,GetProgramFullPath,DecodeLocale,EncodeLocale}`
  - path-config/locale behavior is smoke-covered for setter/getter round-trips, argv export pointer updates, and decode/encode locale round-trip with `PyMem_RawFree` ownership semantics.
- Added Stable-ABI exports and baseline semantics for lifecycle/pack helper APIs (`batch67`):
  - `Py_{Initialize,InitializeEx,Finalize,FinalizeEx,IsFinalizing,NewInterpreter,EndInterpreter,PACK_FULL_VERSION,PACK_VERSION}`
  - lifecycle/pack behavior is smoke-covered for finalize callback execution ordering, finalizing-state reset, interpreter pointer baseline, and packed-version bit layout parity.
- Added Stable-ABI exports and baseline semantics for internal ref/GC helper APIs (`batch68`):
  - `_Py_{IncRef,DecRef,SetRefcnt,NegativeRefcount,CheckRecursiveCall,Object_GC_NewVar,Object_GC_Resize}`
  - internal ref/GC behavior is smoke-covered for refcount mutation helpers, recursion-check baseline, GC var-object allocation/resize baseline, and negative-refcount error signaling.
- Added Stable-ABI exports and baseline semantics for runtime/unicode closure APIs (`batch69`):
  - `Py_{Main,BytesMain,CompileString,Exit,FatalError}`, `PyWrapper_New`
  - `PyUnicode_{AsCharmapString,AsMBCSString,BuildEncodingMap,DecodeCharmap,DecodeCodePageStateful,DecodeMBCS,DecodeMBCSStateful,DecodeUTF7,DecodeUTF7Stateful,EncodeCodePage,Resize,Translate}`
  - runtime/unicode behavior is smoke-covered for compile entry, codec paths, charmap/build-map flows, translate, and resize semantics.
- Manifest normalization now handles Mach-O private-symbol prefixing (`__Py_*` -> `_Py_*`) so abi3 coverage accounting on macOS does not undercount private Stable-ABI symbols.
- Added Stable-ABI exports and semantics for import APIs:
  - `PyImport_{AddModuleRef,AddModuleObject,AddModule,GetModule}`
  - `PyImport_ImportModuleNoBlock`
  - `PyImport_{ImportModuleLevelObject,ImportModuleLevel}`
  - `PyImport_ReloadModule`
- Added Stable-ABI exports and semantics for module-definition APIs:
  - `PyModule_{FromDefAndSpec2,ExecDef,GetDef,GetState}`
  - module-def mapping and `m_size > 0` state allocation are now wired for C-API-created module objects.
- Added Stable-ABI exports and semantics for sys APIs:
  - `PySys_{SetObject,GetXOptions,AddXOption,HasWarnOptions,ResetWarnOptions,AddWarnOption,AddWarnOptionUnicode,WriteStdout,WriteStderr,FormatStdout,FormatStderr,Audit,AuditTuple,SetArgv,SetArgvEx,SetPath}`
  - sys attribute mutation, warn/xoption state, argv/path mutation, and formatted stdout/stderr sink paths now flow through the runtime sys substrate.
- Added Stable-ABI exports for eval-frame APIs:
  - `PyEval_{EvalFrame,EvalFrameEx}`
  - current behavior intentionally guards null/current-frame paths; full CPython frame-evaluation semantics are still tracked as open.
- Added Stable-ABI exports and baseline semantics for thread-state lifecycle APIs:
  - `PyThreadState_{New,Swap,Clear,Delete,DeleteCurrent,SetAsyncExc}`
  - runtime now tracks opaque C-ABI thread-state tokens and supports create/swap/delete flows used by extension-side lifecycle probes.
- Added Stable-ABI exports and baseline semantics for marshal APIs:
  - `PyMarshal_{ReadObjectFromString,WriteObjectToString}`
  - marshal C-API now round-trips core tuple/scalar payloads and rejects invalid payloads with error state.
- Added ABI conformance coverage:
  - symbol gates:
    - `tests/abi3_surface.rs::exports_abi3_batch2_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch3_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch4_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch5_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch6_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch7_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch8_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch9_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch10_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch11_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch12_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch13_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch14_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch15_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch16_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch17_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch18_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch19_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch20_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch21_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch22_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch23_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch24_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch25_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch26_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch27_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch28_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch29_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch30_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch31_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch32_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch33_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch34_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch35_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch36_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch37_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch38_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch39_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch40_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch41_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch42_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch43_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch44_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch45_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch46_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch47_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch48_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch49_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch50_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch51_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch52_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch53_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch54_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch55_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch56_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch57_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch58_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch59_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch60_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch61_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch62_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch63_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch64_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch65_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch66_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch67_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch68_symbols`
    - `tests/abi3_surface.rs::exports_abi3_batch69_symbols`
  - behavior gates:
    - `tests/extension_smoke.rs::cpython_compat_list_set_exception_gc_and_float_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_bytes_error_and_cfunction_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_import_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_error_state_and_file_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_long_abi_batch6_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_long_abi_batch7_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_long_abi_batch8_native_bytes_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_buffer_abi_batch9_helpers_work`
    - `tests/extension_smoke.rs::cpython_compat_sequence_abi_batch10_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_slice_abi_batch11_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_iter_and_memoryview_abi_batch12_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_object_abi_batch13_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_object_buffer_abi_batch14_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_object_gc_and_async_abi_batch15_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_mapping_abi_batch16_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_module_abi_batch17_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_module_helpers_abi_batch18_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_exception_factory_abi_batch19_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_number_abi_batch20_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_error_abi_batch21_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_import_error_abi_batch22_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_warning_abi_batch23_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_import_magic_abi_batch24_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_descriptor_abi_batch25_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_parse_abi_batch26_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_eval_abi_batch27_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_bytes_abi_batch28_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_calliter_abi_batch29_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_gilstate_abi_batch30_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_eval_frame_maps_abi_batch31_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_state_introspection_abi_batch32_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_import_exec_abi_batch33_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_eval_frame_abi_batch34_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_eval_code_abi_batch35_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_import_frozen_abi_batch36_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_frame_abi_batch37_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_file_fromfd_abi_batch38_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_module_def_state_abi_batch39_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_sys_abi_batch40_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_eval_frame_abi_batch41_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_threadstate_abi_batch42_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_marshal_abi_batch43_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_sys_abi_batch44_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_codec_abi_batch45_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_interpreterstate_abi_batch46_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_pystate_abi_batch47_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_member_abi_batch48_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_structseq_abi_batch49_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_pyos_abi_batch50_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_exceptions_abi_batch51_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_data_symbols_abi_batch52_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_thread_abi_batch53_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_type_abi_batch54_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_traceback_abi_batch55_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_error_abi_batch56_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_abi_batch57_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_codec_abi_batch58_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_append_compare_abi_batch59_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_widechar_abi_batch60_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_codec_abi_batch61_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_core_ref_and_identity_abi_batch62_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_unicode_fromformat_abi_batch63_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_weakref_abi_batch64_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_runtime_misc_abi_batch65_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_pathconfig_and_locale_abi_batch66_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_lifecycle_and_pack_abi_batch67_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_internal_ref_gc_abi_batch68_apis_work`
    - `tests/extension_smoke.rs::cpython_compat_runtime_unicode_abi_batch69_apis_work`
- Added Stable-ABI exports and semantics for error/file APIs:
  - `PyErr_{GetRaisedException,SetRaisedException,GetHandledException,SetHandledException,GetExcInfo,SetExcInfo}`
  - `PyFile_{GetLine,WriteObject,WriteString}`
  - `PyExc_EOFError` exported for `PyFile_GetLine` EOF parity paths.
- Added Stable-ABI exports and semantics for long conversion APIs:
  - constructors: `PyLong_{FromSize_t,FromInt32,FromUInt32,FromInt64,FromUInt64}`
  - extractors: `PyLong_{AsInt,AsInt32,AsUInt32,AsInt64,AsUInt64,AsSize_t,AsDouble}`
  - overflow/negative error paths are smoke-covered in the batch6 extension probe.
- Added Stable-ABI exports and semantics for long mask/int-info parsing APIs:
  - `PyLong_{AsUnsignedLongMask,AsUnsignedLongLongMask,FromString,GetInfo}`
  - mask wrap semantics (`-1 -> UINT64_MAX`, large-int modulo) and parse/error paths are smoke-covered in the batch7 extension probe.
- Added Stable-ABI exports and semantics for long native-byte APIs:
  - `PyLong_{AsNativeBytes,FromNativeBytes,FromUnsignedNativeBytes}`
  - endian/required-size/reject-negative/unsigned-buffer/from-bytes signed-vs-unsigned paths are smoke-covered in the batch8 extension probe.
- `PyLong_*` stable-ABI closure is now complete in the current manifest.
- Added Stable-ABI exports and semantics for buffer helper APIs:
  - `PyBuffer_{FillContiguousStrides,FillInfo,IsContiguous,GetPointer,SizeFromFormat,FromContiguous,ToContiguous}`
  - contiguous-stride fill, pointer arithmetic, non-contiguous copy/round-trip, and format-size paths are smoke-covered in the batch9 extension probe.
- Added Stable-ABI exports and semantics for sequence APIs:
  - `PySequence_{Length,GetSlice,SetItem,DelItem,SetSlice,DelSlice,List,Count,Index,In}`
  - `PyObject_DelItem`
  - sequence count/index now compare by runtime value equality (not raw pointer identity), and list slice mutation paths are covered in the batch10 extension probe.
- Added Stable-ABI exports and semantics for slice index APIs:
  - `PySlice_{GetIndices,GetIndicesEx}`
  - strict legacy index extraction and `GetIndicesEx` unpack+adjust paths are covered in the batch11 extension probe.
- Added Stable-ABI exports and semantics for iterator + memoryview APIs:
  - `PyIter_NextItem`, `PyIter_Send`
  - `PyMemoryView_{FromMemory,FromBuffer,GetContiguous}`
  - header baseline now includes `PyIter_Check`, `PyIter_Next`, and `PySeqIter_New` declarations used by extension probes.
  - iterator progression/send-return and memoryview buffer/read-write-contiguous paths are covered in the batch12 extension probe.
- Added Stable-ABI export and semantics for callable-iterator constructor API:
  - `PyCallIter_New`
  - runtime now has a dedicated `IteratorKind::CallIter` path that invokes callable/sentinel progression through `PyIter_Next`.
  - callable/sentinel progression and non-callable rejection are covered in the batch29 extension probe.
- Added Stable-ABI export and semantics for GIL state thread-state query API:
  - `PyGILState_GetThisThreadState`
  - returns current thread-state pointer baseline used by extension code paths that query GIL state.
  - pointer non-null and stability across ensure/release are covered in the batch30 extension probe.
- Added Stable-ABI exports and semantics for eval-frame mapping APIs:
  - `PyEval_GetGlobals`, `PyEval_GetLocals`
  - both now return current frame mapping pointers (or `NULL` without raising when no frame is active), aligned with CPython baseline behavior.
  - global `__name__` visibility and locals-length access are covered in the batch31 extension probe.
- Added Stable-ABI exports and semantics for interpreter/thread-state introspection APIs:
  - `PyInterpreterState_{Get,GetID,GetDict}`
  - `PyThreadState_{GetInterpreter,GetID,GetDict}`
  - state-dict pointers are cached and stable per context, and state-introspection pointer/id/dict round-trips are covered in the batch32 extension probe.
- Added Stable-ABI exports and semantics for import exec/importer APIs:
  - `PyImport_{ExecCodeModule,ExecCodeModuleEx,ExecCodeModuleObject,ExecCodeModuleWithPathnames,GetImporter}`
  - code-object execution now wires through module creation + in-module exec semantics with `__file__` propagation for pathname forms and module cleanup on execution failure.
  - importer lookup uses `pkgutil.get_importer` semantics when available and falls back to `None` without raising, matching CPython-style optional-importer behavior.
- Added Stable-ABI exports and semantics for eval/frame helper APIs:
  - `PyEval_{GetFrame,GetFrameBuiltins,GetFrameGlobals,GetFrameLocals,GetFuncName,GetFuncDesc}`
  - frame/global/local/builtins helpers now return active-frame mappings through the VM substrate, and callable-name/descriptor helpers expose CPython-compatible call-site metadata slices for extension diagnostics.
- Added Stable-ABI exports and semantics for eval-code APIs:
  - `PyEval_{EvalCode,EvalCodeEx}`
  - `EvalCode` now evaluates code objects with explicit globals/locals mappings through VM eval semantics, and `EvalCodeEx` baseline supports the simple no-args/no-closure call path (extended args/defs/closure remain pending).
- Added Stable-ABI exports and semantics for frozen/inittab import APIs:
  - `PyImport_{AppendInittab,ImportFrozenModule,ImportFrozenModuleObject}`
  - inittab entries now register native module init functions and feed import resolution, while frozen import helpers return CPython-style status codes (`1` success, `0` not found baseline, `-1` on API/setup errors).
- Added Stable-ABI exports and semantics for frame-inspection APIs:
  - `PyThreadState_GetFrame`, `PyFrame_GetCode`, `PyFrame_GetLineNumber`
  - frame helper baseline now resolves the active frame through thread-state pointer flow and exposes code-object + current-line metadata used by extension-side diagnostics/introspection.
- Added Stable-ABI export and semantics for file-from-fd constructor API:
  - `PyFile_FromFd`
  - maps CPython-compat API calls onto runtime `_io` open semantics for integer file descriptors with mode/buffering/encoding/errors/newline/closefd handling in the common path.
- Added Stable-ABI exports and semantics for object convenience APIs:
  - `PyObject_{CallNoArgs,CallMethodObjArgs,DelAttr,DelAttrString,DelItemString,Dir,GetOptionalAttrString,HasAttr,HasAttrWithError,HasAttrStringWithError,Length,Repr,SetAttr}`
  - C varargs shim now exports `PyObject_CallMethodObjArgs` (`src/vm/capi_variadics.c`) and is retained in the binary export table.
  - attr-presence helpers now classify missing-attribute paths without converting them into runtime-error failures.
- Added Stable-ABI exports and semantics for legacy object/buffer helpers:
  - `PyObject_{ASCII,Calloc,CheckReadBuffer,AsReadBuffer,AsWriteBuffer,AsCharBuffer,CopyData}`
  - legacy read/write buffer helpers are now backed by CPython-compat object storage (not direct Rust Vec pointers), and copy paths sync destination storage back into runtime values.
- Added Stable-ABI exports and semantics for object/GC helper APIs:
  - `PyObject_{GetAIter,GetTypeData,HashNotImplemented}`
  - `PyObject_GC_{IsTracked,IsFinalized}`
  - behavior coverage currently targets async-iter error path + GC/hash semantics; heaptype type-data offset semantics are tracked for later closure.
- Added Stable-ABI exports and semantics for mapping + async-check helper APIs:
  - `PyAIter_Check`
  - `PyMapping_{Check,Size,Length,GetItemString,Keys,Items,Values,GetOptionalItem,GetOptionalItemString,SetItemString,HasKeyWithError,HasKeyStringWithError,HasKey,HasKeyString}`
  - `PyMapping_GetOptionalItem*` now returns `0` on missing-key paths while preserving non-`KeyError` failures, and `PyMapping_HasKey*` wraps/suppresses failures on legacy APIs.
- Added Stable-ABI exports and semantics for module helper APIs:
  - `PyModule_{NewObject,New,GetNameObject,GetName,GetFilenameObject,GetFilename,SetDocString,Add}`
  - module creation now seeds CPython-style baseline globals (`__name__`, `__doc__`, `__package__`, `__loader__`, `__spec__`) and module filename getters now follow missing-file `SystemError` behavior.
- Added Stable-ABI exports and semantics for module registration helpers:
  - `PyModule_{AddFunctions,AddType}`
  - `PyModule_AddFunctions` now registers CPython `PyMethodDef` tables through the native callable substrate; `PyModule_AddType` now readies the type and publishes it under CPython-style short type name.
- Added Stable-ABI exports and semantics for exception factory helpers:
  - `PyErr_{NewException,NewExceptionWithDoc}`
  - `PyExceptionClass_Name`
  - `PyErr_NewException*` now routes class creation through runtime `type(...)` semantics and injects `__module__` when missing, avoiding unsafe native `PyTypeObject*` assumptions for runtime-owned classes.
- Added Stable-ABI exports and semantics for numeric helper APIs:
  - `PyNumber_MatrixMultiply`
  - `PyNumber_InPlace{Add,Subtract,Multiply,MatrixMultiply,FloorDivide,TrueDivide,Remainder,Power,Lshift,Rshift,And,Or,Xor}`
  - `PyNumber_ToBase`
  - in-place numeric APIs now route through runtime parity ops, matrix-multiply exposes the runtime `@` error surface for unsupported operands, and `PyNumber_ToBase` now supports CPython base-set `{2,8,10,16}` including prefixed/negative formatting with explicit invalid-base errors.
- Added Stable-ABI exports and semantics for error helper APIs:
  - `PyErr_SetFromErrnoWithFilename*`
  - `PyErr_SetExcFromWindowsErr*`, `PyErr_SetFromWindowsErr*`
  - `PyErr_SetInterrupt`, `PyErr_SetInterruptEx`
  - `PyErr_SyntaxLocation`, `PyErr_SyntaxLocationEx`, `PyErr_ProgramText`
  - filename/object-form errno/windows helpers now route through consistent typed OSError-style error state, interrupt helpers set active error state with validation, and syntax/program-text helpers expose basic filename/line diagnostics plus source-line extraction.
- Added Stable-ABI exports and semantics for import-error helper APIs:
  - `PyErr_SetImportError`
  - `PyErr_SetImportErrorSubclass`
  - import-error helpers now validate `ImportError` subclass requirements, require `msg`, instantiate exception objects, and stamp `name`/`path`/`name_from` attributes directly on the raised instance before publishing error state.
- Added Stable-ABI exports and semantics for warning helper APIs:
  - `PyErr_WarnExplicit`
  - `PyErr_ResourceWarning`
  - warning helpers now expose explicit-origin and resource-warning entry points; explicit warnings render origin metadata, and resource warnings route through warning-category dispatch with null-format validation.
- Added Stable-ABI exports and semantics for import magic helper APIs:
  - `PyImport_GetMagicNumber`
  - `PyImport_GetMagicTag`
  - import magic helpers now return CPython 3.14-aligned `.pyc` magic token (`0x0A0D0E2B`) and cache tag (`cpython-314`) for extension-side import metadata probes.
- Added Stable-ABI exports and semantics for descriptor constructor helper APIs:
  - `PyDescr_NewMethod`
  - `PyDescr_NewClassMethod`
  - `PyDescr_NewMember`
  - `PyDescr_NewGetSet`
  - descriptor constructors now allocate CPython-compat descriptor objects, enforce core validation/error paths (`bad call flags`, `Py_RELATIVE_OFFSET` rejection), and are wired into type-dict attribute binding for method/classmethod/getset/member descriptors.
- Added Stable-ABI descriptor type export:
  - `PyClassMethodDescr_Type`
- Added Stable-ABI exports and semantics for parse/kwargs helper APIs:
  - `PyArg_Parse`
  - `PyArg_VaParse`
  - `PyArg_ValidateKeywordArguments`
  - `PyArg_Parse` now follows CPython old-style compat-mode argument counting (`0-arg` and `single-arg` modes, with `"old style getargs format uses new features"` rejection for unsupported forms), `PyArg_VaParse` enforces tuple-backed new-style parsing, and keyword validation now enforces dict-only + string-key semantics.
- Added Stable-ABI exports and semantics for eval call/thread helper APIs:
  - `PyEval_{AcquireLock,ReleaseLock,AcquireThread,ReleaseThread,InitThreads,ThreadsInitialized}`
  - `PyEval_CallObjectWithKeywords`
  - `PyEval_CallFunction`
  - `PyEval_CallMethod`
  - call wrappers route through the same C-side varargs substrate as `PyObject_CallFunction`/`PyObject_CallMethod`; thread helpers now provide concrete CPython-aligned state transitions (GIL acquire/release token semantics, `SaveThread`/`RestoreThread`, thread-state attach/detach validation, and idempotent init/initialized reporting in the single-runtime model).
- Added Stable-ABI signal/recursion control closure updates:
  - `PyErr_CheckSignals`
  - `Py_EnterRecursiveCall`
  - `Py_LeaveRecursiveCall`
  - `_Py_CheckRecursiveCall`
  - `PyErr_CheckSignals` now consumes pending interrupts posted by `PyErr_SetInterrupt` / `PyErr_SetInterruptEx` and raises `KeyboardInterrupt` (`-1`) on check; recursion APIs now follow CPython 3.14 non-overflow path semantics (success/no-op behavior in normal stack conditions), with extension coverage updated in batch21 and batch68 probes.
- Added Stable-ABI exports and semantics for bytes formatting/repr/escape APIs:
  - `PyBytes_FromFormat`
  - `PyBytes_FromFormatV`
  - `PyBytes_Repr`
  - `PyBytes_DecodeEscape`
  - bytes format APIs now follow CPython-style format token handling (`%%`, `%c`, `%d`, `%u`, `%ld`, `%lu`, `%zd`, `%zu`, `%i`, `%x`, `%s`, `%p`) including pointer-prefix normalization and invalid-token pass-through behavior; `PyBytes_Repr` now emits CPython-style repr strings with smart-quote behavior; `PyBytes_DecodeEscape` is wired through runtime escape-decode semantics with strict/replace error paths.

Interpretation:
- Lane A abi3 symbol closure is complete for CPython 3.14 (`functions 782/782`, `data 143/143`).
- Lane B remains the critical path for NumPy/scientific-stack bring-up because current NumPy artifacts are not abi3 wheels.
