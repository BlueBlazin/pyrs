# Stub and Partial Implementation Ledger

This is the canonical ledger for incomplete runtime/stdlib behavior.
No partial surface may remain untracked.

Status values:
- `OPEN`
- `IN_PROGRESS`
- `CLOSED`

## Enforcement
- No-op inventory artifact: `docs/NOOP_BUILTIN_INVENTORY.txt`
- No-op inventory gate: `tests/noop_inventory.rs`
- C-API no-op inventory: `docs/CAPI_NOOP_INVENTORY.md`
- C-API no-op drift gate: `scripts/check_capi_noop_inventory.py`
- Refresh command:
  - `cargo run --quiet --bin print_noop_inventory > docs/NOOP_BUILTIN_INVENTORY.txt`
  - `python3 scripts/check_capi_noop_inventory.py --manifest perf/capi_noop_inventory.json`
- Process/quality gates:
  - `docs/ENGINEERING_GATES.md`
  - `docs/ALGO_AUDIT_BACKLOG.md`

## Milestone 13 P0 Blockers

| Surface | Gap summary | Closure criteria | Required evidence | Status | Milestone |
|---|---|---|---|---|---|
| `pickle`/`pickletools`/`copyreg` | Deferred strict pickle harness lane still times out. | Deferred strict pickle harness lane is re-enabled, green, and allowlist is empty. | `PYRS_RUN_DEFERRED_PICKLE=1 cargo test -q --test cpython_harness runs_cpython_deferred_pickle_suite` | IN_PROGRESS | 13 |
| `_io` | Strict stdlib still depends on remaining `_io` edge semantics. | Remaining `_io`-dependent strict-harness-lane failures are closed. | strict harness pass + targeted `tests/vm.rs` regressions for each closed edge | IN_PROGRESS | 13 |
| `json` | Common workflows are green; long-tail semantics/hardening/perf closure is open. | `json` pure + accelerator parity closed, malformed-input differential coverage closed, perf baseline recorded. | `test_json` parity + differential probes + benchmark artifact | IN_PROGRESS | 13 |
| `_csv`/`csv` | Common workflows are green; long-tail dialect/error parity is open. | `test_csv` parity closed with malformed-input hardening and perf baseline. | `test_csv` parity + differential probes + benchmark artifact | IN_PROGRESS | 13 |
| `_sre` | Core surface works; long-tail regex behavior blocks full pure-`re` closure (notably capture-group/non-capturing-group behavior used by translation snapshot extraction in stdlib tests). | Pure `Lib/re/*` path passes curated/strict harness lanes in scope. | strict/curated harness green for `re`-dependent suites | IN_PROGRESS | 13 |
| CPython compat wrapper lifetime model | CPython-object wrapper ownership currently mixes context-scoped free behavior with extension refcount-scoped usage, creating use-after-free risk in repeated scientific-stack call paths. | VM-global CAPI object registry + explicit borrowed/new/stolen ownership model is active for extension-facing wrappers; no escaped/shared wrapper is freed from `ModuleCapiContext::drop`; stress loops show no stale-pointer/UAF failures and bounded live-entry growth. | `docs/CAPI_LIFETIME_MODEL.md` closure checklist + NumPy repeat-loop stress regressions + sanitizer lane artifacts | IN_PROGRESS | 15 |
| `_ctypes`/`ctypes` | Temporary shim substrate is active (`shims/_ctypes.py`, shim-precedence enabled for `_ctypes`) so SciPy no longer hard-blocks on `ModuleNotFoundError`, but native semantics are incomplete and scientific-stack imports remain unstable (`numpy.random` metaclass/register mismatch + crashy paths). | Replace shim path with native `_ctypes` substrate aligned to CPython behavior, remove shim precedence, and close downstream scientific-stack import failures/crashes. | Direct-mode repro + gate artifact (`perf/numpy_gate_direct_latest.json`) show `_ctypes` no longer shim-backed and `scipy`/`pandas` imports advance without crashes. | IN_PROGRESS | 15 |
| `.pyc` exception-table execution | Exception-table-driven translated-`.pyc` execution is now active (`PUSH_EXC_INFO`/`POP_EXCEPT`/`WITH_EXCEPT_START`/`RERAISE`/`CHECK_EXC_MATCH` + table-driven unwind/dispatch), and startup `import site` no longer needs source fallback for this gap. Remaining work is `.pyc` long-tail opcode/state parity. | Keep exception-table translated-`.pyc` paths green and close remaining `.pyc` long-tail opcode/state gaps so in-scope startup/import paths stay on `.pyc` without fallback. | pyc preference probes (`PYRS_IMPORT_PREFER_PYC=1`) plus targeted translated-`.pyc` regression tests covering `try`/`except`/`with`/`finally` flows | IN_PROGRESS | 13/14 |
| `_ssl`/`ssl` | `_ssl` baseline is landed and a native bootstrap `ssl` module closes extended matrix import/common smoke. Incremental symbol parity has been expanded (`Purpose` and errno-backed import blockers), but full CPython `Lib/ssl.py` path remains blocked by missing enum/symbol/object-model depth (for example `TLSVersion` family + namedtuple/super semantics). | Replace temporary bootstrap `ssl` module with CPython `Lib/ssl.py` as default path, while keeping common-usecase smoke green and adding regression coverage for the namedtuple/super closure. | Extended probe (`perf/stdlib_compat_extended_latest.json`) shows `ssl` import/smoke green + targeted `tests/vm.rs` coverage + object-model closure evidence. | IN_PROGRESS | 13 |
| `pyexpat`/`xml` | Native/runtime `pyexpat` baseline is installed (`ParserCreate`, `ExpatError`, parser callbacks) and `shims/pyexpat.py` is removed. `_elementtree` native extension is currently forced unavailable (`ImportError`) so `xml.etree.ElementTree` uses CPython's pure-Python fallback until `pyexpat.expat_CAPI` is implemented. | Implement CPython-compatible `pyexpat.expat_CAPI` capsule semantics and re-enable `_elementtree` extension import path while keeping XML common-smoke and parse-error regressions green. | `tests/vm.rs::xml_elementtree_fromstring_smoke_uses_native_pyexpat`, `tests/vm.rs::xml_elementtree_imports_with_elementtree_extension_unavailable`, `tests/vm.rs::pyexpat_parse_error_exposes_code_lineno_offset`, strict/extended suites green. | IN_PROGRESS | 13/15 |
| Hash containers (`dict`/`set`/`frozenset`) | Architecture upgrade landed; long-tail semantic/perf closure remains. | CPython parity on edge behavior and performance closure criteria from readiness/audit docs. | targeted parity tests + benchmark/profile artifacts | IN_PROGRESS | 13/14 |
| Builtin symbol surface (`builtins`) | Parity gate currently green. | Keep `145/145`, zero probe mismatches, and empty allowlists. | `./scripts/run_builtin_parity_gate.sh` | CLOSED | 13 |

## Active Non-P0 Partial Surfaces

| Surface | Gap summary | Closure criteria | Status | Milestone |
|---|---|---|---|---|
| Extension ecosystem substrate (`.pyrs-ext` + shared-library loading) | Manifest-backed loader, direct shared-object loading (`.so/.dylib/.pyd`), compiled extension smoke coverage, tagged-filename resolution, C-API v1 slice (module setters/getters/import/attr-load + positional/keyword callable registration + init-scoped object handles/type getters/generic len-getitem helpers/sequence+dict access + iterator helpers + list/dict mutation helpers + object attribute get/set/del/has + type relation checks (`isinstance`/`issubclass`) + handle-based callable invocation (`object_call`, `object_call_noargs`, `object_call_onearg`) + capability probe + import-time error state + error message inspection), and `_sysconfigdata__*` extension-build var baseline (now with compile+import smoke) are landed. Direct CPython-style `PyInit_<module>` fallback now executes through an initial single-phase compatibility slice (`PyModule_Create2`, `PyModule_AddObjectRef`, `PyModule_AddIntConstant`, `PyModule_AddStringConstant`, `PyLong/PyFloat/PyUnicode/PyBytes/PyBool` constructors, `PyErr_*`, `Py_[X]IncRef/Py_[X]DecRef`). Current partial compatibility notes: `PyEval_EvalCodeEx` currently supports only the simple no-args/no-closure call form (extended args/defs/closure semantics remain open), `PyEval_EvalFrame`/`PyEval_EvalFrameEx` currently provide null/current-frame guard behavior but not full CPython frame-evaluation semantics, codec handler APIs (`PyCodec_*Errors`, `PyCodec_LookupError`) currently provide strict type-guard + baseline tuple behavior but not full CPython Unicode-error transformation semantics, `PyMember_SetOne` currently omits CPython truncation/runtime-warning emission for narrowing or negative-to-unsigned writes (value-write baseline is implemented), `PyStructSequence_*` currently provides baseline type/new/item APIs without CPython hidden-field (`n_in_sequence` vs real-field) storage semantics, newly-landed `PyOS_*` APIs currently provide baseline/no-op semantics for fork/interrupt/stack paths (not full runtime-integrated signal/fork state semantics), `PyThread_*` baseline currently uses compatibility semantics for some runtime details (`PyThread_get_thread_native_id` mirrors runtime thread id, `PyThread_GetInfo` returns a tuple baseline instead of CPython structseq object, and `PyThread_exit_thread` uses a non-returning park loop), new `PyType_*` batch54 baseline currently provides compatible heap-type/module/token/slot closure but still uses simplified semantics for full metaclass conflict checks, complete slot-table breadth, and cache invalidation internals, `PyTraceBack_{Here,Print}` batch55 currently provides baseline null-guard/render-write behavior without full traceback object construction semantics, `PyUnicode*Error_*` batch56 currently provides baseline decode-create/getter/setter semantics (including start/end clipping and UnicodeError type-guard compatibility) without full CPython internal UnicodeError storage invariants, Unicode utility APIs batch57 currently provide baseline conversion/equality/read/find/count/split/join/partition semantics without full codec/deprecated Unicode C-API closure, Unicode codec/FS helpers batch58 currently provide baseline decode/encode/FS-converter closure with UTF-8/default-locale compatibility semantics (plus `rot_13` decoded-object compatibility) but not full CPython locale-codec and deprecated Unicode C-API depth, Unicode append/intern/compare helpers batch59 currently provide baseline append pointer-update semantics and rich-compare behavior but not CPython’s full in-place intern table semantics and NotImplemented ref-path invariants, Unicode wide-char helpers batch60 currently provide baseline wide decode/encode and in-place write paths but not full CPython low-level resize/charmap/deprecated-wide API depth, Unicode codec helpers batch61 currently provide baseline escape/UTF16/UTF32 encode+decode helpers (including stateful consumed + byteorder outputs) but not full CPython charmap/MBCS/UTF7/deprecated Unicode API depth, core ref/identity/vectorcall helpers batch62 currently provide baseline identity predicates, ref helper pointers, type-pointer extraction, and vectorcall nargs masking but not full macro-level CPython API breadth, and Unicode varargs formatting helpers batch63 currently provide baseline `PyUnicode_FromFormatV` coverage via the shared bytes-format engine but not full CPython format mini-language parity. Remaining work: broad CPython C-ABI surface closure, type-object/exception-object exports, PEP 489 multi-phase lifecycle, and scientific-stack execution without shims/bridge. | `docs/EXTENSION_CAPABILITY_MATRIX.md` tracks required C runtime + lifecycle surfaces, `tests/extension_smoke.rs::imports_direct_cpython_style_single_phase_extension` is green, and `perf/numpy_gate_direct_latest.json` tracks remaining direct-mode failures. | IN_PROGRESS | 15 |
| CPython-compatible error-value object materialization | Native error-state hooks are stable (message + type) but currently keep null error-value pointers for some paths (`error_set`/`PyErr_SetString`), which avoids crashy pseudo-unicode pointers but is not full CPython `PyErr_*` value semantics. | Restore full non-null CPython-compatible error-value object behavior (including fetch/restore/raised/handled round-trips and message payload fidelity) without regressions or segfaults in extension smoke/harness lanes. | IN_PROGRESS | 15 |
| Importlib/resources/pkgutil helpers | `pkgutil` fallback is now native (`get_data`, `resolve_name`, `iter_modules`, `walk_packages`) and local shim is removed; `importlib.resources` shim is retired and imports now resolve via CPython stdlib path. | Keep CPython stdlib resource/packaging paths green (including `functools.singledispatch` plain `@register` annotation flow used by `importlib.resources`). | IN_PROGRESS | 13 |
| `inspect`/`types` | Foundational behavior exists; bootstrap inspect now exports `isabstract` (unblocks `test_abc` import), but full CPython `inspect.py`/`types.py` parity remains open. | Full stdlib-required behavior parity in scope. | IN_PROGRESS | 13 |
| `threading`/`signal`/`_thread`/`_warnings` | Foundations exist; behavior depth is incomplete. `concurrent.futures` common smoke is now green after iterator protocol + semaphore bound fixes. Current known gap: `threading.Thread.start()` still runs targets through the synthetic-thread path (no true concurrent execution), so contention-sensitive tests such as `test_functools.TestLRUPy.test_lru_cache_threaded2` remain mismatched. | Full in-scope behavioral parity in strict/curated suites beyond common smoke paths, including real contention semantics for threading-sensitive stdlib tests. | IN_PROGRESS | 13/16 |
| `socket`/`_socket` | Baseline exists; native `_scproxy` substrate is now present for urllib/ssl proxy import paths on macOS, but long-tail socket/proxy API behavior remains. | Full in-scope API and behavior parity. | IN_PROGRESS | 13 |
| `unicodedata`/`ucd_3_2_0` | `normalize`/`east_asian_width` plus baseline `category`/`bidirectional` exports are present, but surrogate codepoint fidelity remains constrained by current UTF-8 string storage model (impacts legacy 3.2 category edge semantics such as `Cs` checks in `stringprep`). | Surrogate-preserving text representation and legacy Unicode table parity close `stringprep`/related lanes without compatibility shortcuts. | IN_PROGRESS | 13/14 |
| `uuid` | Foundation exists; long-tail parity remains. | Full in-scope API parity. | IN_PROGRESS | 13 |
| `_sqlite3`/`sqlite3` | Baseline is broad; DB-API long-tail remains. Bytes/bytearray database arguments now flow to sqlite using raw bytes (no lossy UTF-8 replacement), path-like database arguments are accepted, connection+cursor thread-affinity checks are enforced when `check_same_thread=True`, `Connection.iterdump()` is wired through CPython `Lib/sqlite3/dump.py::_iterdump`, `Connection.backup()` is now implemented on top of SQLite backup APIs with CPython-like target/progress/name/pages/sleep semantics, `autocommit` attribute transitions are implemented (`True`/`False`/`LEGACY_TRANSACTION_CONTROL`) with context-manager/commit/rollback semantics, `connect(factory=...)` now relays kwargs and preserves `Base Connection.__init__ not called.` parity for defective subclasses, `Connection.cursor(factory=...)` now follows CPython callable/class dispatch (`factory` positional/keyword semantics, callable return-type validation, and native `Cursor.__init__` substrate), `set_authorizer()` / `set_progress_handler()` / `set_trace_callback()` / `create_collation()` are now backed by native sqlite callback APIs (including callback-traceback and keyword-deprecation behavior used by CPython tests), and `Row` mapping/order/hash behavior now matches `test_factory` expectations (`dict(Row)`, ordering-TypeError paths, stable hash parity for equal rows). Strict stdlib suite runs `test/test_sqlite3/test_dbapi.py`, `test/test_sqlite3/test_backup.py`, `test/test_sqlite3/test_factory.py`, `test/test_sqlite3/test_dump.py`, `test/test_sqlite3/test_transactions.py`, and `test/test_sqlite3/test_hooks.py` green. | Close remaining DB-API long-tail (broader type/threading-runtime parity depth and remaining strict-scope module surfaces such as types/userfunctions/regression). | IN_PROGRESS | 13 |
| `decimal`/`_pydecimal` | Bootstrap `decimal` remains as no-stdlib fallback, but CPython `Lib/decimal.py` is now preferred when available on `sys.path`; targeted pure decimal constructor/context/addition smoke is green. | Refresh extended matrix artifact and close remaining decimal long-tail parity in curated/strict suites. | IN_PROGRESS | 13 |
| `dataclasses`/`typing`/`enum`/`contextvars` | Common paths are green; CPython `Lib/enum.py` path is baseline-green/default and local `enum` shim is retired. Recent typing deltas closed `_typing.NoDefault` singleton semantics, `typing.no_type_check` foreign-function/lambda behavior, overload-registry stability via CPython-shaped nested function `__qualname__`, bound-method forwarding for `__override__`, property attribute write constraints (`property` has no dynamic `__dict__` attrs except `__doc__`), ParamSpec `args`/`kwargs` dir+repr+exec-module+subclassing parity, typing type-parameter base-type error text parity, protocol `__dict__` metadata filtering (`__classdictcell__`/`__classcell__` hidden), collections-ABC subclass fallback parity for builtin container bases (`Sized`/`Container`/`Collection` via `typing._SpecialGenericAlias` -> `collections.abc.*` paths), `Hashable` parity for mutable builtin containers (`issubclass(list, typing.Hashable)` now false), empty-runtime-protocol instancecheck closure for module objects (`isinstance(typing, EmptyProtocol)` no longer raises `issubclass() arg 1 must be a class`), callable-protocol instancecheck parity (`isinstance(function, HasCallProtocol)` now true via `inspect.getattr_static(..., '__call__')` visibility), io bootstrap `Reader`/`Writer` generic subscription parity (`io.Reader[bytes]` / `io.Writer[bytes]` now subscriptable), runtime protocol hook visibility for builtin `str` methods (`str.__dict__` now exposes `startswith`/`endswith` entries used by `_proto_hook`), `object.__new__` constructor-error type parity (`TypeError` instead of runtime wrapper errors for wrong argument shapes), builtin float protocol-member visibility (`float.__dict__` now exposes `__trunc__` for runtime protocol hooks), typing-union identity/repr metadata parity (`typing.Union`/`types.UnionType` aliasing plus union instance `__origin__`/`__module__`/`__name__`/`__qualname__`), `_typing._idfunc` module attribution parity, typing marker non-iterability/message parity (`object is not iterable`), type-parameter default parity for `default=None`, TypeVar bound/constraint validation parity, empty-tuple singleton identity parity for typing default tuples, TypeVarTuple/Unpack identity equality parity in generic-alias comparisons (removed name-based alias equality fallback), TypeVarTuple pickling roundtrip parity for tuple/unpack aliases, parser/compiler class-base starred unpack preservation (`class C(*Ts)` no longer degrades to bare `Ts`), vararg star-annotation lowering parity (`*args: *T` now materializes CPython-shaped runtime annotation values), TypeVarTuple repr parity within unpacked generic aliases (`typing.Unpack[Ts]` instead of `typing.Unpack[*Ts]`), and TypedDict metaclass-call binding parity (`_TypedDictMeta.__call__ = dict` stays unbound so kwargs-only construction works). Current typing failfast frontier is `test.test_typing.TypedDictTests.test_inheritance_pep563` (ForwardRef module attribution still mismatches CPython for one TypedDict inheritance future-annotation path). | Full in-scope semantics for modern pure-Python apps. | IN_PROGRESS | 13 |
| `hashlib` extended algorithms (`_sha1`/`_blake2`/`_sha3`/`_hashlib`) | md5/sha2 baseline and full `_blake2` constructor parameter-block semantics are closed; `_hashlib._constructors`, SHA3 metadata attrs, SHAKE length bounds, and scrypt maxmem validation are now aligned with CPython `test_hashlib` resource-disabled lane. | Full in-scope algorithm surface (or explicit exclusions) with tests and consumers green. | IN_PROGRESS | 13/14 |
| Compression extensions (`zlib`/`_bz2`/`_lzma`) | Native baselines now close common import/one-shot flows for `gzip`/`bz2`/`lzma`; streaming/filter-property long-tail semantics remain partial. | Strict/curated suites and extended probe remain green while placeholder filter APIs are retired. | IN_PROGRESS | 13 |
| Object-model protocol dispatch | Truthiness/membership baseline landed; long-tail slot/error semantics remain. `bytes.lstrip`/`bytes.strip` parity now covers `gzip.decompress` common path, memoryview typed scalar index/store format semantics are landed, first-axis multidim memoryview slice/tolist shape+stride parity is landed, and memoryview byte-export/iteration now honor strided layout + typed decode; remaining memoryview multi-dimensional indexing/slice-assignment long-tail behavior is still open. | Align remaining protocol edge semantics with CPython data model/tests. | IN_PROGRESS | 13 |
| NumPy proxy scalar/array rendering | Direct-mode NumPy import/core gates are green. Scalar baseline parity for `np.float64` constructor/`str`/`repr`/`format` is closed in core paths (`0.5`/`np.float64(0.5)` output). `ndarray` `str/repr` now run through NumPy's real `arrayprint` path (the temporary `tolist()` fallback has been removed) and regression coverage is in `tests/vm.rs::numpy_float_ndarray_repr_does_not_fall_back_to_instance_placeholder`. Remaining gap: long-tail display parity (exact spacing/precision/line-break defaults) is still not fully CPython-identical. | Close remaining `arrayprint` long-tail display parity and keep regression coverage green without VM-level library special-casing. | IN_PROGRESS | 15 |
| VM/module decomposition | VM still has large modules. | Continue concern-based extraction with behavior-preserving tests. | IN_PROGRESS | 14 |

## Strict Harness Lane Accounting
- Active strict suite: `tests/cpython_suite_strict_stdlib.txt`
- Deferred strict pickle suite: `tests/cpython_suite_deferred_pickle.txt`
- Active strict allowlist: `tests/cpython_allowlist_strict.txt` (target: empty)
- Deferred strict pickle allowlist: `tests/cpython_allowlist_deferred_pickle.txt` (target: empty)

Policy:
1. Active strict harness lane stays green with empty allowlist.
2. Deferred strict pickle harness lane stays explicit until re-enabled and closed.
3. Deferred strict pickle harness lane remains opt-in locally (`PYRS_RUN_DEFERRED_PICKLE=1`) for bounded fast loops.

## Local Shim Retirement Checklist

Shims are temporary bootstrap fallbacks and are not allowed to shadow CPython `Lib/` when it is available.

| Shim surface | Current state | Closure criteria | Status |
|---|---|---|---|
| `enum` | Local `shims/enum.py` is removed; enum resolution now follows CPython `Lib/enum.py` only. | Keep `tests/vm.rs::cpython_enum_path_supports_member_value_and_name` and strict stdlib suites green without enum-specific fallback toggles. | CLOSED |
| `pkgutil` | Local shim is removed (`shims/pkgutil.py` deleted); stdlib-less fallback is now native runtime (`pkgutil` builtin module surface). | Keep fallback resource and resolve-name regressions green without filesystem shim fallback. | CLOSED |
| `importlib.resources` | Local shim is removed (`shims/importlib/resources.py` deleted); module now resolves through CPython stdlib path when available. | Keep CPython-stdlib-backed `importlib.resources` paths green and prevent shim fallback reintroduction. | CLOSED |
| `pyexpat` | Local shim is removed (`shims/pyexpat.py` deleted); runtime-native parser baseline is active. | Keep XML parser regressions green on native path. | CLOSED |

## Remaining Intentional NoOp Scope
- Test-only CPython helper modules (`_testcapi`, `_testinternalcapi` family)
- `sys._jit` scaffolding

These entries must not silently expand.

## Update Rules
1. New partial behavior must add/update a row in this file in the same commit.
2. Rows may be marked `CLOSED` only with linked regression tests and required performance evidence (where applicable).
3. Milestone 13 cannot close while any P0 blocker row here is not `CLOSED`.
