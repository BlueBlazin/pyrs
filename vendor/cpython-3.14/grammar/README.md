# Grammar

We will vendor the official CPython 3.14 grammar from the CPython source tree:

- `Grammar/python.gram`
- `Grammar/Tokens`

The sync script writes `python.gram` and `Tokens` into this directory.
